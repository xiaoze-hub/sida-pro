name: build-push-acr

# 发版构建: tag 推送触发 → 构建镜像 → 推阿里云 ACR(国内拉取快)
# 解决: ACR 代码源构建无法拉取 private 仓库代码的问题
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
  # 2026-09-08 审计: 合并前没有任何 CI 跑过(测试只在打 tag 时执行), 补 PR 门禁
  pull_request:
    branches:
      - main

jobs:
  # P2-27 (2026-09-05 28号审计): 生产镜像构建前门禁 — tsc/pytest 任一红灯, 坏 tag 推不上生产
  # 2026-09-08 审计修复: 原 `pytest | tail -3` 管道退出码取 tail, 全红照样绿;
  # `pip install || true` 依赖装不上也继续 — 门禁形同虚设。
  gates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Shell syntax check (bash -n)
        run: |
          set -euo pipefail
          shopt -s nullglob
          # 0.1 类事故(行内注释截断续行命令)语法层即可发现
          for f in deploy/*.sh scripts/*.sh; do
            bash -n "$f"
            echo "OK $f"
          done
      - name: Deploy script stub test (DOCKER=echo)
        run: |
          set -euo pipefail
          # 0.1: 部署脚本重建参数完整性+先验证后切换顺序+失败不删旧容器, echo 桩断言。
          # 这是防止"注释再次被插回 docker run 续行块"的唯一机制(2026-09-08 停机事故)。
          bash scripts/tests/test_deploy_script.sh
      - name: Secret scan (gitleaks v8.18.4, SHA256 pinned)
        run: |
          set -euo pipefail
          # 固定版本二进制+校验和, 不引入未钉版的第三方 action(0.6 治理项)
          curl -sSfL -o /tmp/gitleaks.tgz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.18.4/gitleaks_8.18.4_linux_x64.tar.gz
          echo "ba6dbb656933921c775ee5a2d1c13a91046e7952e9d919f9bac4cec61d628e7d  /tmp/gitleaks.tgz" | sha256sum -c -
          tar -xzf /tmp/gitleaks.tgz -C /tmp gitleaks
          # 扫工作区全部文件(不依赖 git 历史深度); --redact 防命中内容泄进 CI 日志
          /tmp/gitleaks detect --no-git --redact
      - name: Set up Python 3.11 (与生产一致)
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'
      - name: Backend pytest + coverage ratchet (W2.5/E5)
        run: |
          set -o pipefail
          # W2.5/E6(2026-09-09): 依赖统一走 requirements-lock.txt(生产容器 freeze 的精确版本)
          # lock 不含本仓本地包 marketdata(与 Dockerfile 同口径单独 --no-deps 装)
          python3 -m pip install -q -r requirements-lock.txt pytest-timeout pytest-cov
          python3 -m pip install -q --no-deps -e ./packages/marketdata
          # W2.5/E6 漂移守卫: requirements.txt 新增顶层依赖而未再生 lock → 这里红
          python3 scripts/check_lock_covers_reqs.py
          # W2.1/C3(2026-09-09): 越权静态扫描(与 build-and-push-image.yml 同门禁)
          python3 scripts/check_scoped_queries.py
          # W3.1/D2(2026-09-09): 方言分叉只允许出现在 src/db/ 层
          python3 scripts/check_is_pg_scope.py
          # W2.3/E2(2026-09-09): 联网测试统一打 pytestmark=network, CI 主门禁跳过(-m "not network");
          # 之前无 ignore 清单, 联网文件在 CI 本就会因访问不到行情源而挂/超时。
          # 保留 --timeout=60: 仍未打标的潜在联网用例超时兜底。
          # W2.5/E5: 覆盖率棘轮(基线 scripts/coverage-baseline.json, 只许升不许降)
          python3 -m pytest tests/ -q --timeout=60 -m "not network" \
            --cov=src --cov-report=json:coverage.json --cov-report=term
          python3 scripts/check_coverage_ratchet.py coverage.json
      - name: Frontend gates (tsc + eslint + vitest + ui-rules)
        run: |
          cd frontend
          npm install -g pnpm --silent 2>/dev/null || true
          pnpm install --frozen-lockfile
          pnpm typecheck
          pnpm lint
          pnpm test
          node ../scripts/check_ui_rules.mjs

  build:
    # PR 门禁只跑 gates, 不构建不推送镜像
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs: gates
    permissions:
      contents: read
      packages: read   # 拉取私有 ghcr.io/xiaoze-hub/thsdk-vendor(L2 私有 SDK)
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Read VERSION
        id: ver
        run: |
          # 2026-08-20 修复: 优先用 git ref (tag push 时 GITHUB_REF_NAME="v0.3.2"), 避免 VERSION 文件滞后
          # P2-27 (2026-09-05 28号审计): 用 GITHUB_REF_TYPE=='tag' 判定(旧 refs/tags/ 前缀永不匹配)
          # 导致 push 用错 tag (例如 tag=v0.3.2 但推 v0.3.0)。回退到 VERSION 文件兜底。
          REF_TYPE="${GITHUB_REF_TYPE:-}"
          REF_NAME="${GITHUB_REF_NAME:-}"
          if [[ "$REF_TYPE" == "tag" && "$REF_NAME" == v* ]]; then
            V="$REF_NAME"
          elif [[ "$REF_NAME" == v* ]]; then
            V="$REF_NAME"
          else
            V=$(cat VERSION 2>/dev/null || echo "0.0.0")
          fi
          echo "version=$V" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Login to GitHub Container Registry (thsdk vendor)
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: xiaoze-hub
          # GITHUB_TOKEN 拉不到账号级其他私有包, 用经典 PAT(write:packages)
          password: ${{ secrets.GHCR_PAT }}

      - name: Login to Aliyun ACR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: crpi-mte80ai8o78b1429.cn-shanghai.personal.cr.aliyuncs.com
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            crpi-mte80ai8o78b1429.cn-shanghai.personal.cr.aliyuncs.com/xiaozexwz/xzxwz:${{ steps.ver.outputs.version }}
            crpi-mte80ai8o78b1429.cn-shanghai.personal.cr.aliyuncs.com/xiaozexwz/xzxwz:latest
          build-args: |
            VERSION=${{ steps.ver.outputs.version }}
          # 个人版 ACR 不支持 OCI attestation 清单(unknown manifest class for empty.v1+json)
          provenance: false
          sbom: false
