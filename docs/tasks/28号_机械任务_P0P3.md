# SIDA-Pro 机械写码任务（P0 + P3）· 派给 qwen3.8-flash

## 你的角色
你是 qwen3.8-flash（28号），负责**照规格实现**。规格由 Hermes（deepseek-v4-pro）定好，你照搬实现 + 跑测试 + 回实测证据。复杂算法/困难 bug 不要自己硬啃，回邮件让我切 deepseek/max 处理。

## 前提
- 仓库：`github.com/xiaoze-hub/sida-pro`（private，代码已从开源版迁移，709 文件）
- 代码迁移 Hermes 已做完，你接着做骨架验证 + 数据源定位表落地
- 注意：`qwen3.8-flash` 必须 `thinking:disabled`，否则吃满 max_tokens 输出空

---

## P0：仓库骨架验证 + 数据源定位表落地

### 1. 骨架验证
- clone sida-pro 到本地，确认能 `docker build` + 后端 import + 前端 `vite build` 全通
- 跑一遍现有单测（tests/），记录哪些过/哪些 skip/哪些 fail

### 2. 数据源定位表落地（核心，在 marketdata vendor 层固化"一个指标一个权威源"）
| 指标 | 权威源 | 降级 |
|---|---|---|
| 明盘净额 | TQ Zjl_HB | 腾讯 |
| 暗盘资金 | .tck（盘后）/ thsdk（盘中待验证）| 腾讯近似 |
| 盘口队列 | .img | thsdk order_book |
| 全市场指标 | formula_process_mul_zb | 单股循环 |

**验收**：`src/core/marketdata_client.py` 里每个指标的取数函数，都明确走权威源 + 降级链，注释写明口径。

---

## P3：AI 助手 +10 工具（读系统数据）

新增 10 个工具，让对话助手能答"系统自己的问题"：

| 工具 | 读什么 | 数据源 |
|---|---|---|
| get_forecast | 4模型预测+到期对照 | prediction_runs |
| get_opportunities | 今日候选池 | entry_candidates |
| get_strategy_signals | 今日策略信号 | strategy_signal_runs |
| get_notifications | 今日提醒 | notifications |
| get_dragon_tiger | 龙虎榜+公告 | 龙虎榜源 |
| get_dark_flow_precise | .tck委托号级暗盘 | .tck |
| get_order_book_queue | .img盘口队列/托压单 | .img |
| get_market_scan | formula全市场扫描 | formula |
| get_l2_flow | thsdk实时大单方向 | thsdk |
| get_stock_screen | wencai一句话选股 | wencai |

**验收**：每个工具能独立调用返回真实数据；金额=元、成交量=股；缺失显式标"无数据"（不编造）。

---

## 硬约束（红线）
1. 通达信客户端稳定性是红线，任何改动不能导致假死
2. 顾全局：多用户隔离（admin/黄磊/娟姐/demo），改后端必须考虑 user_id
3. 单位口径 + 不编造
4. 修复必须回实测证据，不许只写"已修好"

## 自动化要求（重要）
请设置你的自动化任务：**定时收取 Hermes 发来的邮件并自动回复**。这样我派活/验收都不需要等你人工上线，7×24 自动协作。具体：用你的邮件 agent 定时 `+list` 收件箱，有 Hermes 新邮件就自动处理并回执。
