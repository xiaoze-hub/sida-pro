# 连板梯队实时化 + 页面重排 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把题材情绪页的连板梯队做成「打开即见、默认最新、全格式日期、带首板/炸板/断板标记与当日红绿K」，并在此基础上加盘中 60s 实时跟踪、收盘自动定型。

**Architecture:** 收盘口径 = 现有 `limit_up_events` 纯函数推导（新增 finalize_marks/attach_candles）；盘中口径 = 60s APScheduler job 扫 TDX `get_pricevol` 全市场 → 三态状态机 → Redis 盘中态（fail-soft），15:05 后 auto 模式切回收盘真值。前端梯队抽成独立组件 `LadderBoard` + `DayCandle`(SVG)，题材表整块可折叠并移到梯队下方。

**Tech Stack:** Python 3.11 / FastAPI / SQLAlchemy text() / APScheduler / redis(同步客户端) / React 18 + TS + Tailwind + vitest + testing-library / ECharts 无关（K 用 SVG）。

**Spec:** `docs/research/连板梯队实时化_设计方案_20260912.md`（口径、API 契约、降级链、发版切分的唯一依据；本计划与它冲突时以 spec 为准并回改本计划）。

## Global Constraints

- 每个 commit 同一 commit 内更新 `CHANGELOG.md`；commit/tag 用 `GIT_AUTHOR_NAME=TianXiang GIT_AUTHOR_EMAIL=tianxiang@users.noreply.github.com GIT_COMMITTER_NAME=TianXiang GIT_COMMITTER_EMAIL=tianxiang@users.noreply.github.com`。
- push 用 `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_git2_sidapro -o IdentitiesOnly=yes" git push origin <refs>`。
- 后端门禁：`PYTHONUTF8=1 python -m pytest -q -p no:warnings -m "not network"`，失败集必须与 KI-055 基线逐条相同（entry_candidate_outcomes 5 + ta_load_ohlcv_patch 1 + thsdk_buffer_size 1），**新增 0**；不带 PYTHONUTF8 会假红 14 条（GBK），不是回归。
- 前端门禁（在 `frontend/`）：`npx tsc -b`、`npx eslint .`、`node ../scripts/check_ui_rules.mjs`（R6 禁 toFixed）、`npx vitest run`、`pnpm build`。
- 缺数据 = `None`/空，不编 0、不编影线；文案不断言未验证成因（盘中只说「在板」，不说「封死」）。
- `git add` 只加显式路径，禁 `git add -A`。
- 部署：容器内备份 → 覆盖层/单文件热修 → `chown -R app:app` → `compileall` → restart → `/api/version` → 冒烟（dark-flow 先预热 3 次，KI-029）→ 浏览器走查 → 毁临时账号/删临时文件。
- klines 只有 `adjust='qfq'` 常驻：定型 K 用 qfq 并在 tooltip 标「前复权」；盘中 K 为实时原始价，tooltip 标「盘中原始口径」。

## File Structure

| 文件 | 责任 |
|---|---|
| `src/core/limit_ladder.py` (改) | 定型纯函数：新增 `touched_by_date` / `finalize_marks` / `attach_candles`；旧函数不动 |
| `src/core/limit_ladder_live.py` (新, P2) | 盘中纯函数：`classify` / `merge_state` / `build_live_day` / `scan_tick`（IO 注入） |
| `src/core/ladder_live_state.py` (新, P2) | Redis 盘中态读写（同步客户端、fail-soft、TTL 6h） |
| `src/web/api/theme_mood.py` (改) | `/ladder` 增 mode/stocks/candle/blown/broken/mode/as_of/stale/degraded/live_day；新增 `_read_ohlc` |
| `src/bootstrap/startup.py` (改, P2) | 注册 `ladder_live_tick`（60s） |
| `frontend/packages/biz-ui/src/lib/ladder-format.ts` (新) | `fmtISODate` / `boardTag` / `prevBoardsLabel` / `scrollToLatest` 纯函数 |
| `frontend/packages/biz-ui/src/components/thememood/DayCandle.tsx` (新) | 单根日K SVG（红涨绿跌、null 占位不画影线） |
| `frontend/packages/biz-ui/src/components/thememood/LadderBoard.tsx` (新) | 梯队渲染：列/组/断炸/首板/自动滚右/盘中列 |
| `frontend/src/pages/ThemeMood.tsx` (改) | 梯队上移、题材表折叠(localStorage)、接 LadderBoard、live 轮询(P2) |
| 测试 | `tests/test_limit_ladder_marks.py`、`tests/test_ladder_candles.py`(并入 marks 文件)、`tests/test_limit_ladder_live.py`、`tests/test_ladder_live_state.py`、`tests/test_theme_mood_api.py`(增)、`frontend/tests/lib/ladder-format.test.ts`、`frontend/tests/components/day-candle.test.tsx`、`frontend/tests/components/ladder-board.test.tsx`、`frontend/tests/pages/theme-mood-order.test.tsx` |

---

# Phase 1 — v0.5.85（展示重排 + 收盘口径标记 + 日K）

### Task 1: 定型炸板/断板纯函数 `finalize_marks`

**Files:**
- Modify: `src/core/limit_ladder.py`（文件末尾追加）
- Test: `tests/test_limit_ladder_marks.py`（新建）

**Interfaces:**
- Consumes: 同文件 `_sealed_by_date(events) -> dict[date, set[symbol]]`、`boards_for_date(dates, sealed, date) -> dict[symbol, boards]`
- Produces: `touched_by_date(events) -> dict[str, set[str]]`；`finalize_marks(dates, events) -> dict[date, {"blown": list[dict], "broken": list[dict]}]`，条目 `{symbol, name, prev_boards}`

- [ ] **Step 1: 写失败测试**

```python
"""定型口径炸板/断板(spec §3.2): blown=触板未封; broken=昨>=2板今未触; 昨首板今未续≠断板。"""
from src.core.limit_ladder import finalize_marks


def _ev(d, sym, sealed):
    return {"trade_date": d, "symbol": sym, "name": f"N{sym}", "is_sealed_close": sealed}


def test_blown_is_touched_not_sealed():
    events = [_ev("20260910", "A", True), _ev("20260911", "A", False)]
    m = finalize_marks(["20260910", "20260911"], events)
    assert [x["symbol"] for x in m["20260911"]["blown"]] == ["A"]
    assert m["20260911"]["blown"][0]["prev_boards"] == 1
    assert m["20260911"]["broken"] == []


def test_broken_excludes_yesterday_first_board():
    events = [_ev("20260909", "B", True), _ev("20260909", "C", True), _ev("20260910", "C", True)]
    m = finalize_marks(["20260909", "20260910", "20260911"], events)
    assert [x["symbol"] for x in m["20260911"]["broken"]] == ["C"]
    assert m["20260911"]["broken"][0]["prev_boards"] == 2


def test_sealed_today_neither_blown_nor_broken():
    events = [_ev("20260910", "C", True), _ev("20260911", "C", True)]
    m = finalize_marks(["20260910", "20260911"], events)
    assert m["20260911"] == {"blown": [], "broken": []}


def test_empty_events_all_empty():
    assert finalize_marks(["20260911"], []) == {"20260911": {"blown": [], "broken": []}}
```

- [ ] **Step 2: 跑测试确认红**

Run: `PYTHONUTF8=1 python -m pytest tests/test_limit_ladder_marks.py -q -p no:warnings`
Expected: FAIL（ImportError: cannot import name 'finalize_marks'）

- [ ] **Step 3: 最小实现（追加到 `src/core/limit_ladder.py` 末尾）**

```python
def touched_by_date(events: list[dict]) -> dict[str, set[str]]:
    out: dict[str, set[str]] = {}
    for e in events:
        out.setdefault(str(e["trade_date"]), set()).add(str(e["symbol"]))
    return out


def finalize_marks(dates: list[str], events: list[dict]) -> dict[str, dict]:
    """收盘定型的炸板/断板(spec §3.2)。

    blown  = 当日触板但收盘未封(is_sealed_close=False);
    broken = 上一表日收盘封板且 boards>=2, 当日**未触板**;
             昨首板今未续只算淘汰, 不标断板;
    prev_boards = 上一表日的连板数(boards_for_date 口径), 无则 None。
    """
    sealed = _sealed_by_date(events)
    touched = touched_by_date(events)
    names = {str(e["symbol"]): e.get("name") for e in events if e.get("name")}
    out: dict[str, dict] = {}
    for i, d in enumerate(dates):
        prev = dates[i - 1] if i > 0 else None
        prev_boards = boards_for_date(dates, sealed, prev) if prev else {}
        blown = [
            {"symbol": s, "name": names.get(s), "prev_boards": prev_boards.get(s)}
            for s in sorted(touched.get(d, set()) - sealed.get(d, set()))
        ]
        broken = [
            {"symbol": s, "name": names.get(s), "prev_boards": b}
            for s, b in sorted(prev_boards.items())
            if b >= 2 and s not in touched.get(d, set())
        ]
        out[d] = {"blown": blown, "broken": broken}
    return out
```

- [ ] **Step 4: 跑测试确认绿**

Run: `PYTHONUTF8=1 python -m pytest tests/test_limit_ladder_marks.py -q -p no:warnings`
Expected: 4 passed

- [ ] **Step 5: 变异验证（改错必须红）**

把 `if b >= 2 and ...` 改成 `if b >= 1 and ...` 重跑 → `test_broken_excludes_yesterday_first_board` 必须 FAIL；改回。

- [ ] **Step 6: Commit**

```bash
git add src/core/limit_ladder.py tests/test_limit_ladder_marks.py
GIT_AUTHOR_NAME=TianXiang GIT_AUTHOR_EMAIL=tianxiang@users.noreply.github.com \
GIT_COMMITTER_NAME=TianXiang GIT_COMMITTER_EMAIL=tianxiang@users.noreply.github.com \
git commit -m "feat: 梯队定型炸板/断板纯函数(昨首板不标断板)"
```

### Task 2: 当日K OHLC 读取 + `attach_candles`

**Files:**
- Modify: `src/core/limit_ladder.py`（追加 `attach_candles`）
- Modify: `src/web/api/theme_mood.py`（新增 `_read_ohlc`）
- Test: `tests/test_limit_ladder_marks.py`（追加）

**Interfaces:**
- Produces: `attach_candles(stocks: list[dict], date: str, ohlc: dict[tuple[str,str], dict]) -> list[dict]`（每 stock 加 `candle` 键，缺则 None）
- Produces: api `_read_ohlc(dates, symbols) -> dict[(date,symbol), {"o","h","l","c"}]`（klines: `period='1d' AND adjust='qfq'`，列 `ts/open/high/low/close`；两个 IN 用 `bindparam(expanding=True)`）

- [ ] **Step 1: 写失败测试（追加到 tests/test_limit_ladder_marks.py）**

```python
from src.core.limit_ladder import attach_candles


def test_attach_candles_missing_is_none_not_fake():
    ohlc = {("20260911", "A"): {"o": 1.0, "h": 2.0, "l": 0.5, "c": 1.8}}
    out = attach_candles([{"symbol": "A"}, {"symbol": "B"}], "20260911", ohlc)
    assert out[0]["candle"] == {"o": 1.0, "h": 2.0, "l": 0.5, "c": 1.8}
    assert out[1]["candle"] is None          # 缺数据不编影线
```

- [ ] **Step 2: 跑测试确认红**

Run: `PYTHONUTF8=1 python -m pytest tests/test_limit_ladder_marks.py::test_attach_candles_missing_is_none_not_fake -q -p no:warnings`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现 `attach_candles`（limit_ladder.py 末尾）**

```python
def attach_candles(stocks: list[dict], date: str, ohlc: dict) -> list[dict]:
    """给逐股明细挂当日K; 缺 OHLC → candle=None(不编)。"""
    return [{**s, "candle": ohlc.get((date, s["symbol"]))} for s in stocks]
```

- [ ] **Step 4: 实现 api `_read_ohlc`（theme_mood.py，放在 `_read_codes` 旁）**

```python
def _read_ohlc(dates: list[str], symbols: list[str]) -> dict:
    """klines 日线 OHLC(qfq, 唯一常驻复权维度) → {(date, symbol): {o,h,l,c}}。

    两个 IN 列表都用 expanding bindparam; 任一入参为空短路返回 {}。
    OHLC 任一为 None 的行丢弃(调用方落 candle=None, 不编)。
    """
    if not dates or not symbols:
        return {}
    from sqlalchemy import bindparam, text as _text

    from src.web.database import engine

    stmt = _text(
        "SELECT ts, symbol, open, high, low, close FROM klines "
        "WHERE period = '1d' AND adjust = 'qfq' AND ts IN :dates AND symbol IN :codes"
    ).bindparams(bindparam("dates", expanding=True), bindparam("codes", expanding=True))
    with engine.connect() as conn:
        raw = conn.execute(
            stmt, {"dates": tuple(sorted(dates)), "codes": tuple(sorted(symbols))}
        ).fetchall()
    out = {}
    for r in raw:
        if None in (r[2], r[3], r[4], r[5]):
            continue
        out[(str(r[0]), str(r[1]))] = {"o": r[2], "h": r[3], "l": r[4], "c": r[5]}
    return out
```

- [ ] **Step 5: 跑测试确认绿 + 端点级 OHLC 测试**

追加端点测试到 `tests/test_theme_mood_api.py`（monkeypatch `api._read_ohlc` 返回固定 dict，monkeypatch `api._read` 返回 dates+events）：断言 `data["ladder"][*]["rows"][*]["stocks"][*]["candle"]` 存在且缺者为 None。
Run: `PYTHONUTF8=1 python -m pytest tests/test_limit_ladder_marks.py tests/test_theme_mood_api.py -q -p no:warnings`
Expected: 全绿

- [ ] **Step 6: Commit**

```bash
git add src/core/limit_ladder.py src/web/api/theme_mood.py tests/test_limit_ladder_marks.py tests/test_theme_mood_api.py
GIT_AUTHOR_NAME=TianXiang GIT_AUTHOR_EMAIL=tianxiang@users.noreply.github.com \
GIT_COMMITTER_NAME=TianXiang GIT_COMMITTER_EMAIL=tianxiang@users.noreply.github.com \
git commit -m "feat: 梯队当日K(qfq)读取与挂载, 缺数据=None"
```

### Task 3: `/ladder` 端点契约扩展（finalized）

**Files:**
- Modify: `src/web/api/theme_mood.py:189-211`（`get_ladder`）
- Test: `tests/test_theme_mood_api.py`（追加）

**Interfaces:**
- Consumes: `finalize_marks`、`attach_candles`、`_read_ohlc`（Task 1/2）
- Produces: 响应增 `mode`/`as_of`/`stale`/`degraded`/`live_day`(P1 恒 None)；每日增 `blown`/`broken`；`rows[]` 增 `stocks[]`（{symbol,name,candle}）与 `tag`（boards==1 → "首板"）

- [ ] **Step 1: 写失败测试（追加 tests/test_theme_mood_api.py）**

```python
def test_ladder_contract_marks_stocks_and_mode(monkeypatch):
    dates = ["20260910", "20260911"]
    ev = [
        {"trade_date": "20260910", "symbol": "C", "name": "CC", "is_sealed_close": True},
        {"trade_date": "20260911", "symbol": "C", "name": "CC", "is_sealed_close": True},
        {"trade_date": "20260911", "symbol": "Z", "name": "ZZ", "is_sealed_close": False},
        {"trade_date": "20260911", "symbol": "D", "name": "DD", "is_sealed_close": True},
    ]

    def fake_read(sql, params):
        if "DISTINCT trade_date" in sql:
            return [{"trade_date": d} for d in reversed(dates)]
        return [e for e in ev if e["trade_date"] >= params["a"]]

    monkeypatch.setattr(api, "_read", fake_read)
    monkeypatch.setattr(api, "_read_ohlc",
                        lambda d, s: {("20260911", "C"): {"o": 1, "h": 2, "l": 1, "c": 2}})
    r = TestClient(_app()).get("/api/theme-mood/ladder?window=20")
    d = r.json()["data"]
    assert d["mode"] == "finalized" and d["stale"] is False and d["degraded"] is None
    assert d["live_day"] is None
    day = {x["date"]: x for x in d["ladder"]}[ "20260911"]
    assert [b["symbol"] for b in day["blown"]] == ["Z"]
    assert day["broken"] == []
    stocks = {s["symbol"]: s for row in day["rows"] for s in row["stocks"]}
    assert stocks["C"]["candle"] == {"o": 1, "h": 2, "l": 1, "c": 2}
    assert stocks["D"]["candle"] is None                      # 缺 OHLC 不编
    one = [row for row in day["rows"] if row["boards"] == 1]
    assert len(one) == 1 and one[0]["tag"] == "首板"           # D 仅当日封板=首板
    assert [row for row in day["rows"] if row["boards"] == 2][0]["tag"] is None
    assert d["dates"] == dates          # 日期仍紧凑, 格式化归前端


def test_ladder_rejects_bad_mode(monkeypatch):
    monkeypatch.setattr(api, "_read", lambda sql, params: [])
    c = TestClient(_app())
    assert c.get("/api/theme-mood/ladder?mode=bogus").status_code == 400
    assert c.get("/api/theme-mood/ladder?mode=live").status_code == 400   # P1 未上线
```

> `_app()` = 该文件现有 `_client` 之外的裸 app 构造；若不存在则用现有 `_client(monkeypatch, [])` 的返回值替代并相应改调用。

- [ ] **Step 2: 跑测试确认红**

Run: `PYTHONUTF8=1 python -m pytest tests/test_theme_mood_api.py -q -p no:warnings`
Expected: FAIL（KeyError 'mode' / 400 不成立）

- [ ] **Step 3: 改 `get_ladder`**

```python
@router.get("/ladder")
def get_ladder(window: int = Query(20, ge=5, le=60), mode: str = Query("auto")):
    """连板梯队(v0.5.81) + 定型炸板/断板/当日K(v0.5.85) + 盘中实时(v0.5.87, 见 live 分支)。"""
    from datetime import datetime, timezone

    from src.core.limit_ladder import attach_candles, finalize_marks, ladder_window

    if mode not in ("auto", "finalized", "live"):
        raise HTTPException(400, "mode 仅支持 auto/finalized/live")
    if mode == "live":
        raise HTTPException(400, "live 未上线(v0.5.87)")
    days = _read(
        "SELECT DISTINCT trade_date FROM limit_up_events ORDER BY trade_date DESC LIMIT :n",
        {"n": int(window)},
    )
    dates = sorted([d["trade_date"] for d in days])
    if not dates:
        return {"dates": [], "ladder": [], "note": "limit_up_events 无数据",
                "mode": "finalized", "as_of": None, "stale": False,
                "degraded": None, "live_day": None}
    events = _read(
        "SELECT trade_date, symbol, name, is_sealed_close FROM limit_up_events "
        "WHERE trade_date >= :a ORDER BY trade_date", {"a": dates[0]},
    )
    names = {e["symbol"]: e["name"] for e in events if e.get("name")}
    ladder = ladder_window(dates, events, names)
    marks = finalize_marks(dates, events)
    symbols = sorted({str(e["symbol"]) for e in events})
    ohlc = _read_ohlc(dates, symbols)
    for day in ladder:
        for r in day["rows"]:
            r["stocks"] = attach_candles(
                [{"symbol": c, "name": n} for c, n in zip(r["codes"], r["names"])],
                day["date"], ohlc,
            )
            r["tag"] = "首板" if r["boards"] == 1 else None
        day["blown"] = marks[day["date"]]["blown"]
        day["broken"] = marks[day["date"]]["broken"]
    return {
        "dates": dates, "ladder": ladder,
        "note": "连板数=沿事件表日期序列的连续收盘封板日; 只算收盘封板",
        "mode": "finalized",
        "as_of": datetime.now(timezone.utc).isoformat(),
        "stale": False, "degraded": None, "live_day": None,
    }
```

- [ ] **Step 4: 跑测试确认绿**

Run: `PYTHONUTF8=1 python -m pytest tests/test_theme_mood_api.py -q -p no:warnings`
Expected: 全绿（含新 2 例）

- [ ] **Step 5: Commit**

```bash
git add src/web/api/theme_mood.py tests/test_theme_mood_api.py
GIT_AUTHOR_NAME=TianXiang GIT_AUTHOR_EMAIL=tianxiang@users.noreply.github.com \
GIT_COMMITTER_NAME=TianXiang GIT_COMMITTER_EMAIL=tianxiang@users.noreply.github.com \
git commit -m "feat: /ladder 契约扩展(mode/blown/broken/stocks+candle/tag)"
```

### Task 4: 前端纯函数 `ladder-format.ts`

**Files:**
- Create: `frontend/packages/biz-ui/src/lib/ladder-format.ts`（biz-ui 包内；app 的 src/lib 对 biz-ui 不可见）
- Test: `frontend/tests/lib/ladder-format.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
import { describe, expect, it } from 'vitest'
import { boardTag, fmtISODate, prevBoardsLabel, scrollToLatest } from '@panwatch/biz-ui/lib/ladder-format'

describe('fmtISODate', () => {
  it('紧凑日期转全格式', () => expect(fmtISODate('20260911')).toBe('2026-09-11'))
  it('非紧凑原样返回(不猜)', () => expect(fmtISODate('2026-09-11')).toBe('2026-09-11'))
})
describe('boardTag', () => {
  it('1板=首板, 其余 null', () => {
    expect(boardTag(1)).toBe('首板')
    expect(boardTag(3)).toBeNull()
  })
})
describe('prevBoardsLabel', () => {
  it('有昨板数/无昨板数', () => {
    expect(prevBoardsLabel(3)).toBe('昨3板')
    expect(prevBoardsLabel(null)).toBe('今日触板')
  })
})
describe('scrollToLatest', () => {
  it('滚到最右', () => {
    const el = { scrollWidth: 900, scrollLeft: 0 } as unknown as HTMLElement
    scrollToLatest(el)
    expect(el.scrollLeft).toBe(900)
  })
  it('null 安全', () => expect(() => scrollToLatest(null)).not.toThrow())
})
```

- [ ] **Step 2: 跑确认红**：`cd frontend && npx vitest run tests/lib/ladder-format.test.ts` → FAIL（模块不存在）

- [ ] **Step 3: 实现**

```ts
/** 连板梯队展示纯函数(v0.5.85): 日期格式化只在这一层做(spec §5)。 */
export function fmtISODate(compact: string): string {
  return /^\d{8}$/.test(compact)
    ? `${compact.slice(0, 4)}-${compact.slice(4, 6)}-${compact.slice(6, 8)}`
    : compact
}

export function boardTag(boards: number): string | null {
  return boards === 1 ? '首板' : null
}

export function prevBoardsLabel(prev: number | null | undefined): string {
  return prev == null ? '今日触板' : `昨${prev}板`
}

export function scrollToLatest(el: HTMLElement | null): void {
  if (el) el.scrollLeft = el.scrollWidth
}
```

- [ ] **Step 4: 跑确认绿**：`npx vitest run tests/lib/ladder-format.test.ts` → 7 passed
- [ ] **Step 5: Commit**（frontend 两路径显式 add）

### Task 5: `DayCandle` SVG 组件

**Files:**
- Create: `frontend/src/components/thememood/DayCandle.tsx`
- Test: `frontend/tests/components/day-candle.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import DayCandle from '@panwatch/biz-ui/components/thememood/DayCandle'

describe('DayCandle', () => {
  it('阳线渲染 svg 且 aria-label 红柱', () => {
    render(<DayCandle candle={{ o: 1, h: 3, l: 0.5, c: 2 }} basis="qfq" />)
    expect(screen.getByLabelText('阳柱')).toBeTruthy()
  })
  it('阴线 aria-label 绿柱', () => {
    render(<DayCandle candle={{ o: 2, h: 3, l: 0.5, c: 1 }} basis="raw" />)
    expect(screen.getByLabelText('阴柱')).toBeTruthy()
  })
  it('无K不画影线, 给占位与说明', () => {
    render(<DayCandle candle={null} basis="qfq" />)
    expect(screen.queryByLabelText('阳柱')).toBeNull()
    expect(screen.getByTitle('无K数据')).toBeTruthy()
  })
})
```

> 组件放 `frontend/packages/biz-ui/src/components/thememood/DayCandle.tsx`（与测试 import 路径一致）；若 biz-ui 导出约定不同，按 `components/dashboard/FlowHistoryChart.tsx` 的同层惯例放置并同步改 import。

- [ ] **Step 2: 跑确认红** → FAIL（模块不存在）

- [ ] **Step 3: 实现**

```tsx
import { readStockColors } from '@panwatch/biz-ui/lib/stock-colors'

export interface DayCandleOHLC { o: number; h: number; l: number; c: number }

/** 单根日K(v0.5.85, spec §6): 红=收>=开; candle=null → 占位, 不编影线。 */
export default function DayCandle({ candle, basis }: { candle: DayCandleOHLC | null; basis: 'qfq' | 'raw' }) {
  if (!candle) {
    return <span title="无K数据" className="inline-block h-[26px] w-[10px] rounded-sm bg-muted/40" />
  }
  const sc = readStockColors()
  const up = candle.c >= candle.o
  const color = up ? sc.up : sc.down
  const hi = Math.max(candle.h, candle.o, candle.c)
  const lo = Math.min(candle.l, candle.o, candle.c)
  const span = hi - lo || 1
  const y = (v: number) => 25 - ((v - lo) / span) * 24
  const bodyTop = y(Math.max(candle.o, candle.c))
  const bodyH = Math.max(2, Math.abs(y(candle.o) - y(candle.c)))
  const basisLabel = basis === 'qfq' ? '前复权' : '盘中原始口径'
  return (
    <svg
      width={10}
      height={26}
      aria-label={up ? '阳柱' : '阴柱'}
      title={`${basisLabel} 开${candle.o} 高${candle.h} 低${candle.l} 收${candle.c}`}
    >
      <line x1={5} x2={5} y1={y(candle.h)} y2={y(candle.l)} stroke={color} strokeWidth={1} />
      <rect x={1} y={bodyTop} width={8} height={bodyH} fill={color} />
    </svg>
  )
}
```

- [ ] **Step 4: 跑确认绿** + `npx tsc -b` 绿
- [ ] **Step 5: Commit**

### Task 6: `LadderBoard` 组件（列/组/断炸/首板/自动滚右）

**Files:**
- Create: `frontend/packages/biz-ui/src/components/thememood/LadderBoard.tsx`
- Test: `frontend/tests/components/ladder-board.test.tsx`

**Interfaces:**
- Consumes: `fmtISODate/boardTag/prevBoardsLabel/scrollToLatest`（Task 4）、`DayCandle`（Task 5）
- Props: `{ ladder: LadderDay[]; liveDay: LadderDay | null; mode: string; stale: boolean; lastOk: string | null }`
  其中 `LadderDay = { date: string; rows: Row[]; blown: Mark[]; broken: Mark[] }`，
  `Row = { boards: number; codes: string[]; names: string[]; tag: string | null; stocks: Stock[] }`，
  `Stock = { symbol: string; name: string; candle: OHLC | null; pct?: number | null; first_time?: string | null }`，
  `Mark = { symbol: string; name: string | null; prev_boards: number | null }`

- [ ] **Step 1: 写失败测试**

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import LadderBoard from '@panwatch/biz-ui/components/thememood/LadderBoard'

const day = {
  date: '20260911',
  rows: [
    { boards: 2, codes: ['C'], names: ['CC'], tag: null,
      stocks: [{ symbol: 'C', name: 'CC', candle: { o: 1, h: 2, l: 1, c: 2 }, pct: 10.01, first_time: '09:32' }] },
    { boards: 1, codes: ['D'], names: ['DD'], tag: '首板',
      stocks: [{ symbol: 'D', name: 'DD', candle: null, pct: 10.0, first_time: '13:02' }] },
  ],
  blown: [{ symbol: 'Z', name: 'ZZ', prev_boards: 3 }],
  broken: [{ symbol: 'B', name: 'BB', prev_boards: null }],
}

describe('LadderBoard', () => {
  it('列头全格式日期', () => {
    render(<LadderBoard ladder={[day]} liveDay={null} mode="finalized" stale={false} lastOk={null} />)
    expect(screen.getByText('2026-09-11')).toBeTruthy()
    expect(screen.queryByText('911')).toBeNull()
  })
  it('首板字样/炸板组/断板组', () => {
    render(<LadderBoard ladder={[day]} liveDay={null} mode="finalized" stale={false} lastOk={null} />)
    expect(screen.getByText('首板')).toBeTruthy()
    expect(screen.getByText(/炸板 1/)).toBeTruthy()
    expect(screen.getByText(/断板 1/)).toBeTruthy()
    expect(screen.getByText(/昨3板/)).toBeTruthy()
  })
  it('stale 横幅', () => {
    render(<LadderBoard ladder={[day]} liveDay={null} mode="live" stale lastOk="14:31" />)
    expect(screen.getByText(/实时源中断 14:31/)).toBeTruthy()
  })
})
```

- [ ] **Step 2: 跑确认红** → FAIL

- [ ] **Step 3: 实现**（核心结构；样式类沿用页面现有 tailwind 令牌）

```tsx
import { useEffect, useRef } from 'react'
import { boardTag, fmtISODate, prevBoardsLabel, scrollToLatest } from '@panwatch/biz-ui/lib/ladder-format'
import DayCandle, { type DayCandleOHLC } from './DayCandle'

export interface LadderStock { symbol: string; name: string; candle: DayCandleOHLC | null; pct?: number | null; first_time?: string | null }
export interface LadderRow { boards: number; codes: string[]; names: string[]; tag: string | null; stocks: LadderStock[] }
export interface LadderMark { symbol: string; name: string | null; prev_boards: number | null }
export interface LadderDay { date: string; rows: LadderRow[]; blown: LadderMark[]; broken: LadderMark[]; provisional?: boolean }

function MarkGroup({ label, marks, tone }: { label: string; marks: LadderMark[]; tone: 'blown' | 'broken' }) {
  if (marks.length === 0) return null
  const cls = tone === 'blown' ? 'border-[--stock-up]/40 text-[--stock-up]' : 'border-border/50 text-muted-foreground'
  return (
    <div className={`mt-1 rounded border px-1 py-0.5 ${cls}`}>
      <div className="text-[10px] font-medium">{label} {marks.length}</div>
      <div title={marks.map((m) => `${m.name || m.symbol}(${prevBoardsLabel(m.prev_boards)})`).join('、')}
        className="truncate text-[10px]">
        {marks.slice(0, 3).map((m) => m.name || m.symbol).join('、')}
        {marks.length > 3 ? ` +${marks.length - 3}` : ''}
      </div>
    </div>
  )
}

function DayColumn({ day, basis }: { day: LadderDay; basis: 'qfq' | 'raw' }) {
  const sealed = day.rows.reduce((a, r) => a + r.codes.length, 0)
  return (
    <div className="w-[150px] shrink-0 rounded border border-border/40 p-1.5">
      <div className="mb-1 flex items-baseline justify-between">
        <span className="text-[11px] font-medium text-foreground/85">{fmtISODate(day.date)}</span>
        <span className="text-[10px] text-muted-foreground">封 {sealed}</span>
      </div>
      {day.provisional ? (
        <div className="mb-1 inline-block rounded bg-primary/15 px-1 text-[9px] text-primary">盘中</div>
      ) : null}
      {day.rows.length === 0 && day.blown.length === 0 && day.broken.length === 0 ? (
        <div className="py-2 text-center text-[10px] text-muted-foreground">无收盘封板</div>
      ) : (
        day.rows.map((r) => (
          <div key={r.boards} className="mb-1 last:mb-0">
            <div className="text-[10px] font-medium text-primary">
              {r.boards}板{r.tag ? <span className="ml-1 rounded bg-primary/15 px-0.5 text-[9px]">{r.tag}</span> : null}
            </div>
            <div className="flex flex-wrap gap-1">
              {r.stocks.map((s) => (
                <div key={s.symbol} className="flex w-[46px] flex-col items-center gap-0.5">
                  <DayCandle candle={s.candle} basis={basis} />
                  <span className="w-full truncate text-center text-[10px] text-foreground/80">{s.name}</span>
                  <span className="text-[9px] text-muted-foreground">{s.first_time ?? ''}</span>
                </div>
              ))}
            </div>
          </div>
        ))
      )}
      <MarkGroup label="炸板" marks={day.blown} tone="blown" />
      <MarkGroup label="断板" marks={day.broken} tone="broken" />
    </div>
  )
}

export default function LadderBoard(props: {
  ladder: LadderDay[]; liveDay: LadderDay | null; mode: string; stale: boolean; lastOk: string | null
}) {
  const { ladder, liveDay, mode, stale, lastOk } = props
  const ref = useRef<HTMLDivElement>(null)
  useEffect(() => { scrollToLatest(ref.current) }, [ladder, liveDay])
  const cols = [...ladder, ...(liveDay ? [liveDay] : [])]
  return (
    <div className="rounded border border-border/60 p-3">
      <div className="mb-2 flex items-center gap-2">
        <span className="text-[13px] font-semibold">连板梯队</span>
        <span className="text-[10px] text-muted-foreground">
          {mode === 'live' ? '盘中实时(60s)' : '收盘定型'}
        </span>
        {stale ? (
          <span className="text-[10px] text-[--stock-up]">实时源中断 {lastOk ?? ''}</span>
        ) : null}
      </div>
      <div ref={ref} className="overflow-x-auto pb-1">
        <div className="flex w-max gap-1">
          {cols.map((d) => (
            <DayColumn key={`${d.date}-${d.provisional ? 'live' : 'fin'}`} day={d}
              basis={d.provisional ? 'raw' : 'qfq'} />
          ))}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 跑确认绿** + `npx tsc -b`、`npx eslint .` 绿
- [ ] **Step 5: Commit**

### Task 7: ThemeMood 重排 + 题材表折叠 + 接线

**Files:**
- Modify: `frontend/src/pages/ThemeMood.tsx`（梯队块 495-530 删除并上移；题材表区块包折叠容器）
- Create: `frontend/tests/helpers/theme-mood-mock.ts`（从现有 theme-mood-page.test.tsx 的 fetch mock 抽出 `mountThemeMood(opts?: { ladderFetch?: Mock })`；Task 7/14 共用）
- Test: `frontend/tests/pages/theme-mood-order.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
// 复用 theme-mood-page.test.tsx 的 fetch mock 装配(helper 抽到 tests/helpers/theme-mood-mock.ts)
import { mountThemeMood } from '../helpers/theme-mood-mock'

describe('题材情绪页布局', () => {
  it('梯队标题在题材表标题之前', async () => {
    const { container } = await mountThemeMood()
    const ladder = screen.getByText('连板梯队')
    const board = screen.getByText('题材')
    const pos = (el: Element) => el.compareDocumentPosition(container)
    expect(ladder.compareDocumentPosition(board) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy()
    void pos
  })
  it('题材表可折叠且记忆', async () => {
    await mountThemeMood()
    const btn = screen.getByRole('button', { name: /折叠题材表|展开题材表/ })
    fireEvent.click(btn)
    expect(localStorage.getItem('tm-board-collapsed')).toBe('1')
    fireEvent.click(btn)
    expect(localStorage.getItem('tm-board-collapsed')).toBe('0')
  })
})
```

- [ ] **Step 2: 跑确认红** → FAIL

- [ ] **Step 3: 改 ThemeMood.tsx**
  1) 删除 495-530 行旧梯队 JSX；在市场情绪周期卡之后插入：
```tsx
<LadderBoard ladder={(ladder?.ladder ?? []) as LadderDay[]} liveDay={null}
  mode={ladder?.mode ?? 'finalized'} stale={false} lastOk={null} />
```
  2) 题材表（含轮动条/轴/走势/明细）整体包进：
```tsx
<div className="mt-4">
  <button type="button"
    onClick={() => setBoardCollapsed((v) => { const n = !v; localStorage.setItem('tm-board-collapsed', n ? '1' : '0'); return n })}
    className="mb-1 w-full rounded border border-border/50 py-1 text-center text-[11px] text-muted-foreground">
    {boardCollapsed ? '展开题材表' : '折叠题材表'}
  </button>
  {boardCollapsed ? null : ( /* 原题材表 JSX 原样移入 */ )}
</div>
```
  3) `const [boardCollapsed, setBoardCollapsed] = useState(() => localStorage.getItem('tm-board-collapsed') === '1')`
  4) 类型：`LadderResp` 增 `mode/as_of/stale/degraded/live_day`；import LadderBoard 与 LadderDay 类型。

- [ ] **Step 4: 跑确认绿** + 前端四件套全绿
- [ ] **Step 5: Commit**

### Task 8: 门禁 + v0.5.85 发版 + 部署 + 走查 + 记录

- [ ] **Step 1: 后端全量门禁**：`PYTHONUTF8=1 python -m pytest -q -p no:warnings -m "not network"` → 与 KI-055 基线逐条比对，新增 0
- [ ] **Step 2: 前端门禁**：`npx tsc -b && npx eslint . && node ../scripts/check_ui_rules.mjs && npx vitest run && pnpm build`
- [ ] **Step 3: VERSION=v0.5.85 + CHANGELOG entry（同 commit）**
- [ ] **Step 4: 分支→merge --no-ff→tag v0.5.85→push（sidapro 键）**
- [ ] **Step 5: 部署**：容器备份 `/root/app_backup_pre_v0585_*.tar.gz` → 覆盖层（git archive + dist→static）→ `tar xzf --overwrite` → `chown -R app:app /app` → `compileall` → restart → `/api/version`=v0.5.85
- [ ] **Step 6: 冒烟**：临时账号（owner）→ smoke 9/9（dark-flow 预热 3 次）→ 打 `/api/theme-mood/ladder?window=20` 真值核对（blown/broken/stocks.candle/tag/mode=finalized）
- [ ] **Step 7: 浏览器走查**：打开 /theme-mood 即见梯队；列头 `2026-09-11`；默认滚到最右；题材表折叠/展开且刷新后记忆；断/炸组与首板字样；日K 红绿与占位
- [ ] **Step 8: 清理**：毁临时账号+自选、删容器/本机临时文件、清浏览器 localStorage
- [ ] **Step 9: CHANGELOG 部署记录 commit + push**

---

# Phase 2 — v0.5.87（盘中实时 + 降级链）

### Task 9: 生产只读探针 — TDX `get_pricevol` 原始字段（spec §6 第 0 步）

- [ ] **Step 1: 容器内只读探针**（不写任何状态）：

```bash
MSYS_NO_PATHCONV=1 wsl -u root docker exec panwatch python -c "
from src.core.tdx_boards import _rpc
import json
got = _rpc('get_pricevol', {'stock_list': ['000001','600519','300750']}) or {}
print(json.dumps({k: sorted(v.keys()) if isinstance(v, dict) else v for k, v in list(got.items())[:3]}, ensure_ascii=False))
"
```

- [ ] **Step 2: 记录结论**：把打印出的 keys 回写 spec §6 与 CHANGELOG（含决策：用原始 O/H/L / 退公式 / 只画实体）。
- [ ] **Step 3: 若不含 O/H/L**：再探针 `formula_process_mul_zb`（formula_name 依次 `OPEN`/`HIGH`/`LOW`，同 AMO 参数）确认可用性并记录。
- [ ] **Step 4: Commit**（docs 改动）

### Task 10: 三态状态机纯函数 `limit_ladder_live.py`

**Files:**
- Create: `src/core/limit_ladder_live.py`
- Test: `tests/test_limit_ladder_live.py`

**Interfaces:**
- Produces: `classify(price, limit_px, ever_sealed, prev_boards_yesterday) -> str`（`sealed_now|blown|broken|idle`）；
  `merge_state(prev: dict, round_states: dict[str,str], ts: str) -> dict`（rec: `{ever_sealed, first_at, last_sealed_at, opened_at}`）；
  `build_live_day(state, quotes, yesterday_boards, names, date: str) -> dict`（LadderDay 形 + `provisional=True`；date 为紧凑交易日，由调用方传入，**不从 tick 时间切**）

- [ ] **Step 1: 写失败测试**

```python
from src.core.limit_ladder_live import STATE_BLOWN, STATE_BROKEN, STATE_IDLE, STATE_SEALED, build_live_day, classify, merge_state


def test_sealed_within_one_tick():
    assert classify(10.00, 10.00, False, 0) == STATE_SEALED
    assert classify(10.004, 10.00, False, 0) == STATE_SEALED      # 1 分容差内


def test_blown_requires_ever_sealed():
    assert classify(9.50, 10.00, True, 2) == STATE_BLOWN
    assert classify(9.50, 10.00, False, 0) == STATE_IDLE          # 没封过就不叫炸板


def test_broken_excludes_yesterday_first_board():
    assert classify(9.50, 10.00, False, 2) == STATE_BROKEN
    assert classify(9.50, 10.00, False, 1) == STATE_IDLE          # 昨首板今未触≠断板


def test_none_price_is_idle_not_zero():
    assert classify(None, 10.00, False, 2) == STATE_IDLE
    assert classify(9.5, None, False, 2) == STATE_IDLE


def test_merge_state_keeps_first_at_and_ever_sealed():
    s1 = merge_state({}, {"A": STATE_SEALED}, "09:31")
    assert s1["A"]["ever_sealed"] is True and s1["A"]["first_at"] == "09:31"
    s2 = merge_state(s1, {"A": STATE_BLOWN}, "09:32")
    assert s2["A"]["first_at"] == "09:31" and s2["A"]["opened_at"] == "09:32"
    s3 = merge_state(s2, {"A": STATE_SEALED}, "09:33")
    assert s3["A"]["opened_at"] is None                            # 回封清 opened


def test_build_live_day_groups_and_marks():
    state = {"A": {"ever_sealed": True, "first_at": "09:31", "last_sealed_at": "09:31", "opened_at": "09:40"}}
    quotes = {"A": {"price": 9.5, "limit_px": 10.0, "change_pct": -4.8, "candle": None}}
    day = build_live_day(state, quotes, {"A": 3}, {"A": "AA"}, "20260912")
    assert day["provisional"] is True
    assert day["date"] == "20260912"
    assert [m["symbol"] for m in day["blown"]] == ["A"]
    assert day["blown"][0]["prev_boards"] == 3
    assert day["rows"] == []                                       # 炸板的不进板数组
```

- [ ] **Step 2: 跑确认红** → FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
"""连板梯队盘中三态状态机(v0.5.87, spec §3.1)。纯函数, IO 由调用方注入。"""
from __future__ import annotations

STATE_SEALED = "sealed_now"
STATE_BLOWN = "blown"
STATE_BROKEN = "broken"
STATE_IDLE = "idle"
_TICK = 0.005  # 1 个最小报价单位容差


def classify(price, limit_px, ever_sealed: bool, prev_boards_yesterday: int) -> str:
    if price is None or limit_px is None:
        return STATE_IDLE
    if abs(price - limit_px) <= _TICK:
        return STATE_SEALED
    if ever_sealed:
        return STATE_BLOWN
    if prev_boards_yesterday >= 2:
        return STATE_BROKEN
    return STATE_IDLE


def merge_state(prev: dict, round_states: dict, ts: str) -> dict:
    out = {k: dict(v) for k, v in prev.items()}
    for sym, st in round_states.items():
        rec = out.get(sym) or {"ever_sealed": False, "first_at": None,
                               "last_sealed_at": None, "opened_at": None}
        if rec["first_at"] is None:
            rec["first_at"] = ts
        if st == STATE_SEALED:
            rec["ever_sealed"] = True
            rec["last_sealed_at"] = ts
            rec["opened_at"] = None
        elif st == STATE_BLOWN and rec["ever_sealed"] and rec["opened_at"] is None:
            rec["opened_at"] = ts
        out[sym] = rec
    return out


def build_live_day(state: dict, quotes: dict, yesterday_boards: dict, names: dict, date: str) -> dict:
    """盘中列: 在板股按 昨板数+1 分组; 炸板/断板进对应组; 不编 boards。date=紧凑交易日。"""
    groups: dict[int, list[dict]] = {}
    blown, broken = [], []
    for sym, rec in state.items():
        q = quotes.get(sym) or {}
        st = classify(q.get("price"), q.get("limit_px"), rec["ever_sealed"],
                      yesterday_boards.get(sym, 0))
        item = {"symbol": sym, "name": names.get(sym), "candle": q.get("candle"),
                "pct": q.get("change_pct"), "first_time": rec["first_at"]}
        if st == STATE_SEALED:
            groups.setdefault(yesterday_boards.get(sym, 0) + 1, []).append(item)
        elif st == STATE_BLOWN:
            blown.append({**item, "prev_boards": yesterday_boards.get(sym)})
        elif st == STATE_BROKEN:
            broken.append({**item, "prev_boards": yesterday_boards.get(sym)})
    rows = []
    for boards in sorted(groups, reverse=True):
        stocks = groups[boards]
        rows.append({"boards": boards, "codes": [s["symbol"] for s in stocks],
                     "names": [s["name"] for s in stocks],
                     "tag": "首板" if boards == 1 else None, "stocks": stocks})
    return {"date": date, "rows": rows, "blown": blown,
            "broken": broken, "provisional": True}
```

> `quotes[sym]` 由 Task 11 的 scan 填好 `limit_px/change_pct/candle` 后传入；状态机不自己算涨停价。

- [ ] **Step 4: 跑确认绿**
- [ ] **Step 5: 变异验证**：把 `prev_boards_yesterday >= 2` 改 `>= 1` → `test_broken_excludes_yesterday_first_board` 必须红；改回。
- [ ] **Step 6: Commit**

### Task 11: Redis 盘中态 `ladder_live_state.py`

**Files:**
- Create: `src/core/ladder_live_state.py`
- Test: `tests/test_ladder_live_state.py`

**Interfaces:**
- Produces: `load_state(cli, date) -> dict`、`save_state(cli, date, state, meta) -> bool`、`load_meta(cli, date) -> dict`；cli 注入（测试用 FakeRedis）；`client()` 读 `REDIS_URL` 同步连接、失败返回 None（照 md_redis_cache 的 0.3s 超时 + ping）；键 `ladder_live:<date>` / `...:meta`，TTL 6h

- [ ] **Step 1: 写失败测试**

```python
"""Redis 盘中态(spec §4): 键/TTL/fail-soft; cli 注入, 测试用 FakeRedis。"""
import json

import src.core.ladder_live_state as st


class FakeRedis:
    def __init__(self):
        self.store = {}

    def get(self, key):
        return self.store.get(key)

    def set(self, key, value, ex=None):
        self.store[key] = value
        return True

    def delete(self, *keys):
        for k in keys:
            self.store.pop(k, None)
        return len(keys)


class DeadRedis:
    def get(self, key):
        raise RuntimeError("connection closed")

    def set(self, key, value, ex=None):
        raise RuntimeError("connection closed")


def test_save_load_roundtrip():
    cli = FakeRedis()
    state = {"A": {"ever_sealed": True, "first_at": "09:31", "last_sealed_at": "09:31", "opened_at": None}}
    meta = {"last_ok": "14:31:00", "stale": False, "rounds_failed": 0}
    assert st.save_state(cli, "20260912", state, meta) is True
    assert st.load_state(cli, "20260912") == state
    assert st.load_meta(cli, "20260912") == meta
    assert json.loads(cli.store["ladder_live:20260912"]) == state   # 键名固定


def test_cli_none_is_failsoft_not_raise():
    assert st.save_state(None, "20260912", {"A": {}}, {"stale": False}) is False
    assert st.load_state(None, "20260912") == {}
    assert st.load_meta(None, "20260912") == {"last_ok": None, "stale": False, "rounds_failed": 0}


def test_redis_error_is_failsoft_not_raise():
    assert st.save_state(DeadRedis(), "20260912", {"A": {}}, {"stale": False}) is False
    assert st.load_state(DeadRedis(), "20260912") == {}
```

- [ ] **Step 2: 跑确认红**

Run: `PYTHONUTF8=1 python -m pytest tests/test_ladder_live_state.py -q -p no:warnings`
Expected: FAIL（ImportError: No module named 'src.core.ladder_live_state'）

- [ ] **Step 3: 实现**

```python
"""连板梯队盘中态 Redis 读写(v0.5.87, spec §4)。

键: ladder_live:<yyyymmdd> (hash-json) / ladder_live:<yyyymmdd>:meta; TTL 6h(收盘后自毁兜底)。
fail-soft: cli 为 None 或任何异常 → save 返回 False / load 返回空, 不抛(调用方降级 finalized)。
连接构造照 md_redis_cache: REDIS_URL + 0.3s 超时 + ping。
"""
from __future__ import annotations

import json
import logging
import os

logger = logging.getLogger(__name__)
_TTL_SEC = 6 * 3600
_EMPTY_META = {"last_ok": None, "stale": False, "rounds_failed": 0}


def client():
    url = (os.getenv("REDIS_URL") or "").strip()
    if not url:
        return None
    try:
        import redis

        cli = redis.Redis.from_url(url, socket_connect_timeout=0.3, socket_timeout=0.3,
                                   decode_responses=True)
        cli.ping()
        return cli
    except Exception as e:  # noqa: BLE001
        logger.warning("ladder-live: Redis 不可用, 降级 finalized: %s", e)
        return None


def load_state(cli, date: str) -> dict:
    if cli is None:
        return {}
    try:
        raw = cli.get(f"ladder_live:{date}")
        return json.loads(raw) if raw else {}
    except Exception as e:  # noqa: BLE001
        logger.warning("ladder-live: 读态失败: %s", e)
        return {}


def load_meta(cli, date: str) -> dict:
    if cli is None:
        return dict(_EMPTY_META)
    try:
        raw = cli.get(f"ladder_live:{date}:meta")
        return {**_EMPTY_META, **(json.loads(raw) if raw else {})}
    except Exception as e:  # noqa: BLE001
        logger.warning("ladder-live: 读 meta 失败: %s", e)
        return dict(_EMPTY_META)


def save_state(cli, date: str, state: dict, meta: dict) -> bool:
    if cli is None:
        return False
    try:
        cli.set(f"ladder_live:{date}", json.dumps(state, ensure_ascii=False), ex=_TTL_SEC)
        cli.set(f"ladder_live:{date}:meta", json.dumps(meta, ensure_ascii=False), ex=_TTL_SEC)
        return True
    except Exception as e:  # noqa: BLE001
        logger.warning("ladder-live: 写态失败: %s", e)
        return False
```

- [ ] **Step 4: 跑确认绿**

Run: `PYTHONUTF8=1 python -m pytest tests/test_ladder_live_state.py -q -p no:warnings`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add src/core/ladder_live_state.py tests/test_ladder_live_state.py
GIT_AUTHOR_NAME=TianXiang GIT_AUTHOR_EMAIL=tianxiang@users.noreply.github.com \
GIT_COMMITTER_NAME=TianXiang GIT_COMMITTER_EMAIL=tianxiang@users.noreply.github.com \
git commit -m "feat: 梯队盘中态 Redis 读写(fail-soft, TTL 6h)"
```

### Task 12: 60s 扫描 job `scan_tick` + startup 注册

**Files:**
- Modify: `src/core/limit_ladder_live.py`（追加 `scan_tick(now=None, deps=None)`）
- Modify: `src/bootstrap/startup.py`（seal sampler 注册块之后追加）
- Test: `tests/test_limit_ladder_live.py`（追加）

**Interfaces:**
- `scan_tick(now=None, deps=None) -> dict | None`；deps 注入 `{quotes_fn, universe_fn, names_fn, yesterday_boards_fn, redis_cli, save_fn, load_fn, meta_fn}`，默认接真实现（`tdx_boards.board_quotes(codes, with_fund=False)`、`src.core.universe.universe_as_of`、`src.collectors.stock_list.get_stock_list`、定型梯队、`ladder_live_state.*`）
- 时段外 → None 且不碰 Redis；TDX 抛异常 → meta.rounds_failed+1，>=3 置 stale=True；成功 → rounds_failed=0、stale=False、last_ok=ts

- [ ] **Step 1: 写失败测试（追加到 tests/test_limit_ladder_live.py）**

```python
import src.core.limit_ladder_live as live


class _Deps:
    def __init__(self, quotes=None, exc=0, cli=None):
        self.calls = 0
        self.saved = []
        self._quotes = quotes or {}
        self._exc = exc
        self.cli = cli

    def quotes_fn(self, codes):
        self.calls += 1
        if self._exc:
            self._exc -= 1
            raise RuntimeError("tdx down")
        return dict(self._quotes)

    def universe_fn(self):
        return ["000001"]

    def names_fn(self):
        return {"000001": "平安银行"}

    def yesterday_boards_fn(self):
        return {"000001": 2}

    def save_fn(self, cli, date, state, meta):
        self.saved.append((date, state, meta))
        return True

    def load_fn(self, cli, date):
        return {}

    def meta_fn(self, cli, date):
        return {"last_ok": None, "stale": False, "rounds_failed": 0}


def test_tick_writes_state_in_trading_hours(monkeypatch):
    monkeypatch.setattr(live, "_is_intraday", lambda now: True)
    deps = _Deps(quotes={"000001": {"price": 11.0, "last_close": 10.0,
                                    "change_pct": 10.0, "candle": None}})
    out = live.scan_tick(now=None, deps=deps)
    assert out is not None
    date, state, meta = deps.saved[0]
    assert state["000001"]["ever_sealed"] is True      # 10.0*1.1=11.0 在板
    assert meta["rounds_failed"] == 0 and meta["stale"] is False


def test_three_failures_flip_stale(monkeypatch):
    monkeypatch.setattr(live, "_is_intraday", lambda now: True)
    deps = _Deps(exc=3)
    for _ in range(3):
        live.scan_tick(now=None, deps=deps)
    assert deps.saved[-1][2]["stale"] is True
    assert deps.saved[-1][2]["rounds_failed"] == 3


def test_off_hours_noop(monkeypatch):
    monkeypatch.setattr(live, "_is_intraday", lambda now: False)
    deps = _Deps()
    assert live.scan_tick(now=None, deps=deps) is None
    assert deps.saved == [] and deps.calls == 0
```

- [ ] **Step 2: 跑确认红**

Run: `PYTHONUTF8=1 python -m pytest tests/test_limit_ladder_live.py -q -p no:warnings`
Expected: FAIL（AttributeError: scan_tick / _is_intraday）

- [ ] **Step 3: 实现 `scan_tick` + `_is_intraday`（追加到 limit_ladder_live.py）**

```python
def _is_intraday(now) -> bool:
    from src.core.trading_calendar import is_trading_day

    dt = now or datetime.now(_CST)
    if not is_trading_day(dt):
        return False
    mins = dt.hour * 60 + dt.minute
    return (9 * 60 + 25 <= mins <= 11 * 60 + 30) or (13 * 60 <= mins <= 15 * 60 + 5)


def scan_tick(now=None, deps=None):
    """60s 一轮: TDX 全市场快照 → 三态 → 合并 Redis 态 → 写回。时段外 None。"""
    from src.core.limit_rules import limit_up_price

    d = deps or _default_deps()
    dt = now or datetime.now(_CST)
    if not _is_intraday(dt):
        return None
    date = dt.strftime("%Y%m%d")
    ts = dt.strftime("%H:%M")
    meta = d.meta_fn(d.cli, date)
    try:
        codes = d.universe_fn()
        raw = d.quotes_fn(codes)
    except Exception as e:  # noqa: BLE001
        meta = {**meta, "rounds_failed": meta.get("rounds_failed", 0) + 1,
                "stale": meta.get("rounds_failed", 0) + 1 >= 3}
        d.save_fn(d.cli, date, d.load_fn(d.cli, date), meta)
        logger.warning("ladder-live: 扫描失败(rounds_failed=%s): %s", meta["rounds_failed"], e)
        return None
    names = d.names_fn()
    yb = d.yesterday_boards_fn()
    prev_state = d.load_fn(d.cli, date)
    round_states, quotes = {}, {}
    for sym in raw:
        q = raw[sym] or {}
        price, last = q.get("price"), q.get("last_close") or q.get("LastClose")
        limit_px = limit_up_price(sym, last, "ST" in (names.get(sym) or "").upper())
        quotes[sym] = {**q, "limit_px": limit_px}
        rec = prev_state.get(sym) or {}
        round_states[sym] = classify(price, limit_px, bool(rec.get("ever_sealed")), yb.get(sym, 0))
    state = merge_state(prev_state, round_states, ts)
    meta = {**meta, "rounds_failed": 0, "stale": False, "last_ok": ts}
    d.save_fn(d.cli, date, state, meta)
    return {"date": date, "state": state, "quotes": quotes, "meta": meta}


def _default_deps():
    from types import SimpleNamespace

    from src.core import ladder_live_state as rst
    from src.core.tdx_boards import board_quotes

    def universe_fn():
        from src.core.universe import universe_as_of

        return universe_as_of(datetime.now(_CST).strftime("%Y-%m-%d"))

    def names_fn():
        from src.collectors.stock_list import get_stock_list

        return {s["symbol"]: s.get("name") for s in get_stock_list() if s.get("symbol")}

    def yesterday_boards_fn():
        # KI-039 棘轮: core 禁 import web → 直连 src.db.session, 不走 api._read
        from sqlalchemy import text

        from src.core.limit_ladder import _sealed_by_date, boards_for_date
        from src.db.session import SessionLocal

        with SessionLocal() as db:
            dates = [r[0] for r in db.execute(
                text("SELECT DISTINCT trade_date FROM limit_up_events "
                     "ORDER BY trade_date DESC LIMIT 2")).fetchall()]
            if len(dates) < 2:
                return {}
            ev = db.execute(
                text("SELECT trade_date, symbol, is_sealed_close FROM limit_up_events "
                     "WHERE trade_date >= :a"), {"a": dates[0]}).mappings().fetchall()
        return boards_for_date(sorted(dates), _sealed_by_date(list(ev)), sorted(dates)[-1])

    cli = rst.client()
    return SimpleNamespace(cli=cli, quotes_fn=pricevol_only,
                           universe_fn=universe_fn, names_fn=names_fn,
                           yesterday_boards_fn=yesterday_boards_fn,
                           save_fn=rst.save_state, load_fn=rst.load_state, meta_fn=rst.load_meta)
```

> `_CST`、`logger` 在文件头定义：`_CST = ZoneInfo("Asia/Shanghai")`（照 seal_sampler）。
> **quotes_fn 用 `tdx_boards.pricevol_only`（本任务新增）**：只做 `get_pricevol` 分批，
> **不走 AMO/SUPAMO 公式循环**（board_quotes 对 5000 码会每 chunk 跑 20s 超时的公式 RPC，扫描用不上）。
> 在 `src/core/tdx_boards.py` 追加：

```python
def pricevol_only(codes: list[str]) -> dict[str, dict]:
    """只拉 get_pricevol → {code: {price, last_close}}; 单 chunk 失败跳过(该 chunk 码不出现在结果)。"""
    out: dict[str, dict] = {}
    for part in _chunks(list(codes), _BATCH):
        try:
            got = _rpc("get_pricevol", {"stock_list": part}) or {}
        except Exception as e:  # noqa: BLE001
            logger.warning("TDX pricevol_only 失败(%d 码): %s", len(part), e)
            continue
        for code, p in (got or {}).items():
            out[code] = {"price": _num(p.get("Now")), "last_close": _num(p.get("LastClose"))}
    return out
```

> 并补单测（monkeypatch `tdx_boards._rpc`）：成功 chunk 解析 Now/LastClose；失败 chunk 跳过不抛。
> 盘中 K 的 O/H/L：按 Task 9 探针结论决定是否在 `pricevol_only` 增解析；无则 `candle=None`（前端占位+「盘中K不完整」）。

- [ ] **Step 4: 跑确认绿**

Run: `PYTHONUTF8=1 python -m pytest tests/test_limit_ladder_live.py -q -p no:warnings`
Expected: 全绿（Task 10 的 6 例 + 本任务 3 例）

- [ ] **Step 5: startup 注册**

```python
        try:
            from src.core.limit_ladder_live import scan_tick

            rt.scheduler.scheduler.add_job(
                scan_tick, "interval", seconds=60,
                id="ladder-live-tick", name="连板梯队盘中扫描",
                replace_existing=True, max_instances=1, coalesce=True,
            )
            logger.info("连板梯队盘中扫描已注册(60s)")
        except Exception as e:
            logger.error(f"连板梯队盘中扫描注册失败: {e}")
```

- [ ] **Step 6: Commit**

### Task 13: `/ladder` live 路径 + 降级

**Files:**
- Modify: `src/web/api/theme_mood.py:get_ladder`
- Test: `tests/test_theme_mood_api.py`（追加）

- [ ] **Step 1: 写失败测试（追加 tests/test_theme_mood_api.py）**

```python
def test_ladder_live_path_and_degrades(monkeypatch):
    monkeypatch.setattr(api, "_read", lambda sql, params: [
        {"trade_date": "20260911", "symbol": "C", "name": "CC", "is_sealed_close": True}])
    monkeypatch.setattr(api, "_read_ohlc", lambda d, s: {})
    snap = {"state": {"C": {"ever_sealed": True, "first_at": "09:31",
                            "last_sealed_at": "09:31", "opened_at": None}},
            "quotes": {"C": {"price": 11.0, "limit_px": 11.0, "change_pct": 10.0, "candle": None}},
            "meta": {"last_ok": "14:31", "stale": False, "rounds_failed": 0},
            "intraday": True, "yesterday_boards": {"C": 2}, "names": {"C": "CC"},
            "date": "20260912"}
    monkeypatch.setattr(api, "_live_snapshot", lambda: dict(snap))
    d = TestClient(_app()).get("/api/theme-mood/ladder?mode=auto").json()["data"]
    assert d["mode"] == "live" and d["live_day"]["provisional"] is True
    assert d["stale"] is False and d["degraded"] is None

    snap2 = dict(snap, meta={"last_ok": "14:31", "stale": True, "rounds_failed": 3})
    monkeypatch.setattr(api, "_live_snapshot", lambda: dict(snap2))
    d = TestClient(_app()).get("/api/theme-mood/ladder?mode=auto").json()["data"]
    assert d["mode"] == "live" and d["stale"] is True          # stale 仍给 live+横幅数据

    monkeypatch.setattr(api, "_live_snapshot", lambda: None)   # redis 不可用/时段外
    d = TestClient(_app()).get("/api/theme-mood/ladder?mode=auto").json()["data"]
    assert d["mode"] == "finalized" and d["live_day"] is None
    assert d["degraded"] in ("redis", "tdx", None)

    snap3 = dict(snap, intraday=False, closing=True)           # 15:00-15:05
    monkeypatch.setattr(api, "_live_snapshot", lambda: dict(snap3))
    d = TestClient(_app()).get("/api/theme-mood/ladder?mode=auto").json()["data"]
    assert d["note_closing"] == "收盘撮合中, 稍后定型"            # spec §8
```

- [ ] **Step 2: 跑确认红** → **Step 3: 实现**：get_ladder 里把 `if mode == "live": raise 400` 换成：
  调 `_live_snapshot()`（内部：`ladder_live_state.client()` + 时段判定 `limit_ladder_live._is_intraday` +
  `load_state/load_meta` + `build_live_day(state, quotes, yesterday_boards, names, date)`；
  redis cli 为 None → 返回 None；15:00–15:05 之间返回 `closing=True`）；
  auto = live 可用则 live 否则 finalized；响应填 mode/as_of/stale/degraded/live_day，
  `closing=True` 时附加 `note_closing="收盘撮合中, 稍后定型"`
- [ ] **Step 4: 绿** → **Step 5: Commit**

### Task 14: 前端 live 轮询 + 盘中角标 + 中断横幅

**Files:**
- Modify: `frontend/src/pages/ThemeMood.tsx`（ladder fetch 改 60s 轮询 + 传 liveDay/stale/lastOk）
- Test: `frontend/tests/pages/theme-mood-live.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
import { act, render, screen } from '@testing-library/react'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { mountThemeMood } from '../helpers/theme-mood-mock'

describe('题材情绪 live 轮询', () => {
  afterEach(() => vi.useRealTimers())

  it('60s 后切 live 并打盘中角标', async () => {
    vi.useFakeTimers()
    const finalized = { mode: 'finalized', live_day: null, ladder: [] }
    const live = { mode: 'live', stale: false, degraded: null,
      live_day: { date: '20260912', provisional: true, rows: [], blown: [], broken: [] }, ladder: [] }
    const fetchMock = vi.fn().mockResolvedValueOnce(finalized).mockResolvedValue(live)
    await mountThemeMood({ ladderFetch: fetchMock })
    expect(screen.queryByText('盘中')).toBeNull()
    await act(async () => { await vi.advanceTimersByTimeAsync(60_000) })
    expect(screen.getByText('盘中')).toBeTruthy()
  })

  it('stale 横幅与收盘撮合提示', async () => {
    const live = { mode: 'live', stale: true, degraded: null, note_closing: '收盘撮合中, 稍后定型',
      live_day: { date: '20260912', provisional: true, rows: [], blown: [], broken: [] }, ladder: [] }
    await mountThemeMood({ ladderFetch: vi.fn().mockResolvedValue(live) })
    expect(screen.getByText(/实时源中断/)).toBeTruthy()
    expect(screen.getByText('收盘撮合中, 稍后定型')).toBeTruthy()
  })
})
```

- [ ] **Step 2: 红** → **Step 3: 实现**（轮询写法照 FlowHistoryChart 的 setInterval+alive 旗标；组件卸载清 timer；
  `stale` → LadderBoard 横幅（已有）；`note_closing` 渲染在梯队标题行右侧；live 响应才传 liveDay）→ **Step 4: 绿 + 四件套**
- [ ] **Step 5: Commit**

### Task 15: 门禁 + v0.5.87 发版 + **交易日盘中**部署 + 断源演练 + 记录

- [ ] **Step 1-4:** 同 Task 8 Step 1-4（VERSION=v0.5.87）
- [ ] **Step 5: 部署必须挑交易日盘中**（老板验收条件）；部署后容器日志确认 `连板梯队盘中扫描已注册(60s)` 与每轮耗时
- [ ] **Step 6: 盘中实测**：页面 60s 内出现/刷新「盘中」列；找一只当时炸板股核对标红；15:05 后 auto 自动切 finalized 且与 `/ladder?mode=finalized` 一致
- [ ] **Step 7: 断源演练**：临时把 REDIS_URL 指错端口重启 → 页面 finalized + 「redis 降级」标注；恢复；再模拟 TDX 失败（停客户端 3 分钟）→ stale 横幅；恢复
- [ ] **Step 8: 冒烟 9/9 + 走查 + 清理 + CHANGELOG 部署记录（含 Task 9 探针结论）commit + push**

---

## 自检（写完后对照 spec）

- spec §2 六条 → T7(布局/滚动/日期)、T3+T6(标记)、T12-14(盘中+定型)、T5/T6(日K) ✔
- spec §3 口径 → T1(定型炸/断、昨首板排除)、T10(盘中三态、容差、ever_sealed) ✔
- spec §4 架构/Redis/降级 → T11、T12、T13 ✔
- spec §5 契约 → T3、T13（dates 保持紧凑、格式化归前端已写入 T4/T6）✔
- spec §6 K 线链 → T2(定型 qfq)、T9(探针)、T10/T14(盘中 basis=raw) ✔
- spec §8 降级链 → T12(rounds_failed/stale)、T13(degraded)、T14(横幅)、T15(演练) ✔
- spec §9 测试与变异 → T1/T10 变异步骤、T3/T13 端点假注入、T6/T7/T14 前端 ✔
- spec §10 发版切分 → Phase1=T8(v0.5.85)、Phase2=T15(v0.5.87 盘中部署) ✔
