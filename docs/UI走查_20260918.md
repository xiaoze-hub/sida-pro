# 全站 UI 走查报告 · 2026-09-18 · v0.6.5

**基线**: 生产容器 `/api/version=v0.6.5`，admin JWT 走查，viewport 1440×900  
**截图**: `docs/screenshots/ui-sweep-20260918/`（18 页）+ `report.json`  
**方法**: Playwright 注入 token → 逐页 `networkidle` + 2.5s → fullPage 截图 + 空态文本采集

---

## 一、数据未正常显示（按严重度）

| 级别 | 页面 | 现象 | 判定 |
|---|---|---|---|
| P1 | 首页 KPI | 情绪周期 / 主线 Top1 / 涨停跌停 **加载失败**（琥珀色） | `/market/phase` 超时（诚实态，非静默） |
| P1 | 首页 三卡 | 市场阶段 / 市场主线 / 市场温度 **请求超时** | 同上；右下有「重试」 |
| P2 | 个股工作台 | 盘口快照源「无数据」；十档 0.00万；主力意图/封单/筹码全 `--` | **收盘后预期**（thsdk/TQ 盘中才有），note 已披露 |
| P2 | 个股工作台 | 明盘历史净额 **整列 `--`**（暗盘有值） | 明盘历史日无采集，已标注 |
| P2 | 指数工作台 | 今开/最高/最低 **`--`**，成交量有值 | 腾讯指数 quote 对 OHLC 常返 null → **已修：当日日K回填** |
| P2 | 个人中心 | 「命中 587/1360 · **undefined**」 | `prediction.note` 缺省被模板字符串吞成字面量 → **已修** |
| P3 | 首页 涨跌分布 | 「暂无分布数据」 | 上游未下发；空态文案已存在 |
| P3 | 系统 Agent | 「最近失败: 4」 | 运维问题（非 UI） |
| P3 | 通知 | 近 24h 失败通知 12–23 条反复告警 | push 外部失败；噪音偏高 |
| P3 | 数据源 | 部分源「成功率 0%」 | 源健康，非布局 |

---

## 二、布局 / 空白问题

| 页面 | 问题 | 建议 |
|---|---|---|
| **持仓** | 无持仓时主体区 ~70% 空白，只有一行「暂无持仓」 | **已改**：居中空态 + 两行引导（如何添加 / 去关注页） |
| **个股工作台** | 带2 左列 380px 图 + 右列「数智决策」更高 → 图下方大片空白 | 后续：右列 sticky 或左列图高自适应 |
| **首页** | 三指标共振列表极长（40+ 行），把异动池/发现推得很远 | 建议默认 Top20 + 「展开更多」 |
| **板块热力** | 说明文案一行过长，右侧小格文字被裁切 | 已有保底面积；小格可只显示涨跌幅 |
| **指数工作台** | 今开/最高/最低 原先 `--` | **已修** OHLC 回填 |
| **登录页** | 正常 | — |

---

## 三、已修复（本批）

1. **Profile `undefined`**（`Profile.tsx`）— note 缺省不再拼进文案  
2. **指数 OHLC**（`IndexBody.tsx`）— quote 缺字段时用**当日**最后一根日K回填，非当日不编造  
3. **持仓空态**（`AccountsSection.tsx`）— 居中两行引导，减少死白  

---

## 四、建议下批（未改）

1. 首页三指标共振默认折叠 Top20  
2. 工作台左右列高度对齐 / 右列滚动  
3. 通知页失败告警去重（同文案 24h 只展示最新一条）  
4. 盘中时段再跑一轮走查（L2/主力意图/封单真值路径）  
5. `/market/phase` 超时根治（首页三卡失败）  

---

## 五、截图索引

见 `docs/screenshots/ui-sweep-20260918/01-login.png` … `18-settings-help.png` 与 `report.json`。
