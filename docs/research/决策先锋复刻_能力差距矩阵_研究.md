# SIDA-Pro × 决策先锋 — 三指标复刻能力差距矩阵（研究版）

> 整理日期：2026-09-02 · 整理人：ZCode (Marvis)
> 阶段：Phase 1 研究（只读扫描，未修改任何代码）
> 范围：`C:\Users\tianxiang\sida-pro\`（Hermes 交付的本地副本）+ 已读 4 份核心文档 + 桌面 8 个 TDX 公式源码
> 目的：作为后续 Phase 2 重构规划的依据

---

## 0. 执行摘要（≤300 字）

**SIDA-Pro 后端已具备决策先锋三指标的算法底座。** `decision_pioneer.py`（375 行，复刻引擎内核）+ `gs_strategy.py` + `ai_activity.py` + `resonance.py` + `dark_pool_flow.py` 共同构成了阶段1 五件套，**test_five_pack 完整覆盖 4 态 + 7 行 + 缺输入降级**，与官方规格在算法层面已对齐。

**当前主要差距（按优先级）**：

1. **P0（5 条）**：wencai 三指标共振选股 3 模板缺失；暗盘 1/3/5 日 SERIES + 0 轴 CROSS 未实现；`chat_tools.get_dark_flow_precise` 仍为 TODO 骨架；AIClient 全局熔断只覆盖反证层；暗盘七口诀 ⑥⑦ 已退化为死代码。
2. **P1（5 条）**：实战文案 GO/STOP 字段化；暗盘 SERIES 复用；strong_absorb 三常量统一；盘中暗盘主笔级切线；`_derive_direction` 跨文件复用。
3. **P2（5 条）**：decision_pioneer API 接入；三指标前端展示层补强；wencai 选股模板；chat_tools 工具骨架补齐；治理补强。

**数据源充足**：thsdk 1.7.18（云端 L2）+ TQ more_info + 腾讯 + wencai_nlp + 本地 .tck/.img 五源已固化在 `marketdata_authoritative_sources`。唯一缺口是 thsdk 全市场 1/3/5 日暗盘 API（需走 `formula_process_mul_zb`）。

**未做开发**：仅扫描、规格化、列改造清单；不实施任何代码改动；不提议 PR。

---

## 1. SIDA-Pro 后端架构速写

### 1.1 模块清单（按职责）

```
src/core/
├── decision_pioneer.py     375 行  ★ 复刻引擎内核（GS + 活跃度 + Zjl_HB L2）
├── gs_strategy.py          173 行  ★ 阶段1 五件套 ① GS 信号官方语义
├── ai_activity.py           87 行  ★ 阶段1 五件套 ④ AI 活跃度官方语义
├── resonance.py                       阶段1 五件套 ⑤ 七行状态表
├── dark_pool_flow.py                 主力资金聚合（明盘+暗盘）
├── dark_l2.py                        L2 入口（三源：thsdk/TDX/.tck）
├── dark_flow.py                      腾讯逐笔暗盘 v5（七口诀）
├── dark_split.py                     big_order_flow 方向解码 + 明/暗拆分
├── ohlc_dark.py                      OHLC 分摊（对照项，approximation=True）
├── chip_distribution.py              筹码分布（三层降级）
├── market_scan.py                    全市场三榜扫描
├── marketdata_authoritative_sources.py  ★ P0 数据源定位表（Hermes 派活实现）
├── ai_client.py            260 行  ★ LLM 客户端（含 GlobalLLMCircuitBreaker）
└── chat_tools.py           573 行  AI 助手 10 工具骨架

src/agents/
├── intraday_monitor.py              主力意图（审计标"反证层治理最优"）
├── chart_analyst.py                 K线形态
├── daily_report.py / premarket_outlook.py / news_digest.py
└── chat/
    ├── tools_thsdk.py       21 行  thsdk 薄壳入口
    └── (chat_tools 实际在 src/core/，规格应改写)

src/web/api/
├── klines.py             K线 + summary 聚合（P1 图层数据 09-01 上线）
├── quotes.py             行情 5 档 + more-info + dark-flow-tq
├── stock_pool.py         三指标共振选股 API（/screen）
├── discovery.py          个股发现与归类
└── decision_pioneer.py   1.6KB  当前仅 stub
```

### 1.2 文件名校正（规格与实际不一致）

| 规格中的文件名 | 实际存在？ | 实际路径 | 备注 |
|---|---|---|---|
| `src/core/dark_flow_fusion.py` | ❌ | — | 由 `dark_l2.py` 兼担 |
| `src/core/dark_flow_l2.py` | ❌ | — | 同上 |
| `src/core/dark_fund_scan.py` | ❌ | — | 由 `market_scan.py` 承担 |
| `src/web/api/marketScan.py` | ❌ | `stock_pool.py` / `discovery.py` | 由二者拆分承担 |
| `src/agents/chat/chat_tools.py` | ❌ | `src/core/chat_tools.py` | 实际在 src/core/ |

---

## 2. 决策先锋规格总表（原子化）

> 来源：`决策先锋8问8答_解析.md`（GD）+ `同花顺决策先锋复刻研究手册.md`（FX）+ `通达信客户端研究结果汇总-20260830.md`（TDX）+ `Desktop\同花顺决策先锋核心功能详细解析.md`（ZS）+ `Desktop\三指标公式\*.txt` 8 个 TDX 公式源码

### 2.1 GS 策略（主图）

| 原子 | 规格 | 关键常量 |
|---|---|---|
| G 信号 | 趋势有望启动，优先做多、持股 | — |
| S 信号 | 趋势暂或结束，减仓、清仓为主 | — |
| G 区间 / S 区间 | 多头排列延续期 / 空头排列延续期 | — |
| 二次 G | G 区间内活跃度/资金较前日翻倍 | — |
| 非函数指标 | 信号触发后次日仍显示，直至转 S | — |
| 实战文案（GO） | "GO: 趋势有望启动，建议逢低建仓" | — |
| 实战文案（STOP）| "STOP: 趋势暂或结束，建议减仓避险" | — |
| 简化算法（GSXH.txt）| MA5 上穿 MA20 + 放量 | 5/20 |
| 仿编算法（TDX）| BB0=(MA3+MA7+MA13+MA27)/4 + A0=(H+L+2O+6C)/10 + CROSS | 3/7/13/27 |
| 主图叠加位置 | G 图标在 LOW×0.99 / S 图标在 HIGH×1.01 | 0.99 / 1.01 |
| G/S 区色带 | 紫色短柱（G）/ 绿色短柱（S） | — |

### 2.2 主力资金（副图）

| 原子 | 规格 | 关键常量 |
|---|---|---|
| 明盘资金 | 单笔成交金额 ≥ 30 万的大单净额 | **30 万** |
| 暗盘资金 | AI 识别拆单/对倒/量化单 | — |
| 主力资金 | **明盘净额 + 暗盘净额** | — |
| 散户资金 | -主力资金（镜像） | — |
| 周期维度 | 1 日 / 3 日 / 5 日 | — |
| 多空分界 | 0 轴 | — |
| 多空判定 | 上穿 0 轴 = 多；下穿 0 轴 = 空 | — |
| 红绿柱 | 流入=红柱；流出=绿柱 | — |
| 三日净颜色 | COLORYELLOW | — |
| 五日净颜色 | COLORMAGENTA | — |
| 价量组合 4 种 | 健康持股/出逃回避/吸筹反转/诱多警惕 | — |
| 二次共振 | 资金较前一日翻倍 | ×2 |
| K 线分摊公式 | VOL × (2C-L-H)/(H-L) | — |

### 2.3 AI 机构活跃度（副图）

| 原子 | 规格 | 关键常量 |
|---|---|---|
| 7 因子 | 上影/下影/实上/实下/全幅/涨幅/高开 | — |
| 算法 | MAX(7 因子) × 1.2 | **×1.2** |
| 生命线 | 阈值 | **+1.56** |
| 强势线 | 阈值 | **+3.00** |
| 大牛线 | 阈值 | **+6.00** |
| 三色柱 | 紫（≥6）/ 红（3-6）/ 绿（<3） | — |
| 五均 | MA(活跃度, 5) 洋红线 | — |
| 连强天 | BARSLASTCOUNT(活跃度 > 1.56) | — |
| 强转 | 今日首次跨过 1.56 | — |
| 三步战法口诀 | "要顺不要逆，要活不要死，要红不要绿" | — |

### 2.4 三指标共振（核心实战）

| 原子 | 规格 | 关键常量 |
|---|---|---|
| 七行状态表 | 见 §2.5 | — |
| 向好（首次共振）| G信号 + 强势线上 + 流入 | 上涨概率 **75.42%** |
| 拐点（二次共振）| G区间 + 强线 + 流入（翻倍）| 上涨概率 **73.48%** |
| 分歧（平稳）| G区间 + 强线上 + 流入 | 上涨概率 **70.55%** |
| 走坏（全走坏）| S信号 + 跌破强势线 + 流出 | 上涨概率 **67.40%** |
| 盈亏比 向好/拐点/分歧/走坏 | 3.45 / 3.57 / 3.37 / 3.29 | — |
| 三步战法 | 机会挖掘（3 好）/ 注意风险（3 坏）| — |
| 一句话选股 | 三指标 + 问财语句 | — |

### 2.5 七行状态表（GD §3.4 OCR 截图补全）

| # | 趋势 (GS) | 活跃度 | 主力资金 | 判断 | 操作思路 |
|---|---|---|---|---|---|
| 1 | G信号 | 强势线上 | 流入 | **三指标首次共振** | 注意机会挖掘（向好）|
| 2 | G区间 | 强势线上（较前一日翻倍）| 流入（较前一日翻倍）| **三指标再次共振** | 持续关注机会（拐点）|
| 3 | G区间 | 强势线上 | 流入 | **3 指标平稳** | 关注指标动态（分歧）|
| 4 | G区间 | 强势线上 | 流出 | **3 指标 1 个走坏** | 警惕-主力出货 |
| 5 | G区间 | 跌破强势线 | 流入 | **3 指标 1 个走坏** | 警惕-主力还没出 |
| 6 | G区间 | 跌破强势线 | 流出 | **3 指标 2 个走坏** | 警惕-具体幅度根据行情 |
| 7 | S信号 | 跌破强势线 | 流出 | **三指标全走坏** | 注意潜在风险（走坏）|

---

## 3. 能力差距矩阵（核心）

### 3.1 GS 指标

| 子要素 | SIDA-Pro 现状 | 数据源 | 改造路径 |
|---|---|---|---|
| G 信号（趋势有望启动） | ✅ **已有**（`decision_pioneer.compute_gs_signal` + `gs_strategy.eval_gs` 实现 BB0/A0 上穿 → G；test_five_pack:38-66 覆盖） | TQ `get_market_data period=1d` | 无需改 |
| S 信号（趋势暂或结束） | ✅ **已有**（同函数，下穿→S；test 验证 S 区间/S 信号） | 同上 | 无需改 |
| 非函数指标（次日不消失） | ✅ **已有**（`eval_gs` 实现 `pending` + `signal_confirmed`，末根疑似不报；`trend_label()` 输出"G信号/G区间/S信号/S区间" 4 态）| 同上 | 无需改 |
| 实战文案（GO/STOP）| ⚠️ **部分**（算法层 4 态已分；GO/STOP 文案映射未在 resonance/state API 层做最终翻译；前端文案由前端实现）| — | **P1**：`resonance.py` 末尾增加 `state_action_label()`：行号→中文短句 "GO↑/持续关注/警惕主力出货/注意风险"，作为 API 字段返回 |
| G/S 图标 + G/S 区色带 | ❌ **无**（主图叠加 UI）| — | **P1**：前端 KlineChart 集成 InteractiveKline 主图叠加层 |
| 二次 G 判定（翻倍）| ⚠️ **部分**（`resonance.py` 有 row=2 判定"较前日翻倍"，但活跃度/资金均需 series 输入）| gs_strategy + ai_activity + dark_pool_flow | 依赖 P0 暗盘 SERIES 完成 |

### 3.2 主力资金

| 子要素 | SIDA-Pro 现状 | 数据源 | 改造路径 |
|---|---|---|---|
| 明盘资金（单笔 >30 万）| ✅ **已有**（`dark_split.ming_net_from_big_orders` + `dark_pool_flow._ming_flow` 走 thsdk `big_order_flow`；实测：神剑股份 253 行 100% ≥30 万，与官方扩展1"主力净流入"精确到元一致）| thsdk `big_order_flow`（盘中）| 无需改 |
| 暗盘资金（拆单/对倒识别）| ⚠️ **部分**（`dark_pool_flow._dark_flow` 走 dark_flow 拆单 v4 + `_detect_split_orders` 时间间隔聚类，已对齐同花顺暗盘口径：金健米业案例验证；但**标 `confidence=L1_approx`**，非 AI 模型；thsdk `tick_super_level1` 主笔级主线 2026-08-19 数据已验证、待 09:30 盘中切换）| thsdk `tick_super_level1`（盘中待切）+ `.tck`（盘后）+ 腾讯逐笔（兜底）| **P0**：`chat_tools.get_dark_flow_precise` 实装 .tck 主笔级还原；**P1**：环境开关 `PANWATCH_DARK_SOURCE=thsdk` 盘中主笔级；**P1**：.tck a28/a32 委托号级（主笔级净+898 万 / 真实净+939 万，P0-3 已校准）作为盘后权威 |
| 1/3/5 日周期 | ⚠️ **部分**（`ohlc_dark.ohlc_dark_net(bars, days=N)` 支持 N 日聚合；dark_flow 仅给出当日净额，无 3/5 日聚合；`compute_pool_flow` 单股当日）| K 线（OHLC 周期聚合）| **P0**：新增 `dark_pool_flow.compute_pool_flow_series(symbol, days=1/3/5)`：3/5 日 = sum(daily main_net) + 趋势判定，输出 main_net_series / red_days / green_days；提供 0 轴上穿/下穿事件 |
| 上穿/下穿 0 轴定多空 | ⚠️ **部分**（`resonance.fund_flow_label(net)` 仅给当前 direction/color；无历史序列 + 上穿/下穿事件检测）| K 线 | **P0**：新增 `fund_flow.py:detect_cross(ming_series, dark_series, window=3/5) -> {cross_up, cross_down, last_zero_break}` |
| 红柱/绿柱 | ✅ **已有**（`ohlc_dark.allocate_bar` 给每根 K 的 BarAllocation，包含 buy/sell/net；桌面公式口径：净额柱 = VOL*(2C-L-H)/(H-L)，与 `allocate_bar` 一致）| K 线 | 无需改（口径已对齐）|
| 价量组合 4 种语义 | ⚠️ **部分**（算法层 K线+资金方向可判定，但前端"健康持股/出逃回避/吸筹反转/诱多警惕"文案未实现）| K 线 + 资金柱 | **P1**：resonance API 增加 `pv_combo` 字段，前端按 pv_combo 渲染对应文案 |
| 二次共振（资金翻倍）| ❌ **无**（无 history 比较）| 资金 SERIES | 依赖 P0 暗盘 SERIES 完成 |

### 3.3 AI 机构活跃度

| 子要素 | SIDA-Pro 现状 | 数据源 | 改造路径 |
|---|---|---|---|
| 7 因子 MAX×1.2 | ✅ **已有**（`decision_pioneer.compute_institution_activity` 7 因子：上影/下影/实体+上影/实体+下影/上影+下影/涨幅/高开；max ×1.2；桌面公式 1_JGHYD 实测对齐）| TQ 日 K | 无需改 |
| 生命线 +1.56 | ✅ **已有**（`LIFE_LINE=1.56`、`ai_activity.activity_of_value` 输出 level=生命；test 覆盖 1.0/2.0 临界）| 同上 | 无需改 |
| 强势线 +3.00 | ✅ **已有**（`STRONG_LINE=3.00`；`above_strong` bool 输出；test 覆盖 2.9/3.0 临界）| 同上 | 无需改 |
| 大牛线 +6.00 | ✅ **已有**（`BULL_LINE=6.00`；test 覆盖 7.0=大牛）| 同上 | 无需改 |
| 三色柱（紫/红/绿）| ⚠️ **部分**（算法层输出 zone=DEAD/WEAK/ALIVE/HOT/BULL 5 档，对应 0/1.56/3/6/12+；前端按 zone 染色逻辑待集成）| TQ 日 K | **P1**：前端 KlineChart 副图层集成三色柱 + 三阈值线 |
| 五均 / 连强天 / 强转 | ⚠️ **部分**（算法可算，未作为 API 字段输出）| TQ 日 K | **P1**：`ai_activity.activity_of_value` 增加 `activity_5ma` / `activity_strong_days` / `activity_cross_life` 字段 |

### 3.4 三指标共振选股

| 子要素 | SIDA-Pro 现状 | 数据源 | 改造路径 |
|---|---|---|---|
| G 信号 + 强势线上 + 资金流入（首次共振）| ✅ **已有**（`resonance.evaluate_state` row=1 已实装，phase=向好，回测基准 win_rate 0.7542；test_five_pack 110-112 覆盖）| gs_strategy + ai_activity + dark_pool_flow | 无需改 |
| 7 行状态表完整支持 | ✅ **已有**（row 1~7 全实现 + backtest 4 态基准已固化；test 覆盖 110-145 完整 7 行 + 缺输入降级）| 同上 | 无需改 |
| 全市场扫描 | ⚠️ **部分**（`market_scan.scan()` 已实现：今日新出 G 点榜 / 暗盘 TOP（OHLC 对照 hard mark）/ 机构活跃度 TOP + ZLJC 批量；但 dark_top 用的是 OHLC 对照，非真实暗盘——与决策先锋 "选股 = 暗盘资金持续流入" 铁律不符；thsdk 全市场 `formula_process_mul_zb` 批量未接入）| decision_pioneer + ohlc_dark（OHLC 对照）/ thsdk formula_process_mul_zb（待接入）| **P0**：替换 `market_scan.dark_top` 为 `dark_pool_flow.compute_pool_flow(symbols, days=1)` 实际算，逐股 fallback OHLC；**P1**：接入 `thsdk.formula_process_mul_zb(formula=三指标共振)` 全市场批量 |
| wencai 三指标共振选股 3 模板 | ❌ **无**（`chat_tools.WENCAI_TEMPLATES` 21 个模板齐全，但缺决策先锋"买共振/卖共振/选股共振" 3 条核心；桌面公式 5_GZZH 已写好通达信公式，自然语言版缺失）| wencai_nlp | **P0**：新增 3 模板：`买共振：今日 G信号 且 活跃度大于3 且 暗盘资金连续3日红色`、`卖共振：今日 S信号 且 活跃度较前日下降 且 暗盘资金连续3日绿色`、`选股共振：暗盘资金持续流入 且 活跃度大于6 且 G信号` |
| 实战文案（向好/拐点/分歧/走坏）| ⚠️ **部分**（`evaluate_state` 返回 phase 枚举；前端文案需 API 字段化）| resonance | **P1**：增加 `state_action_label()` 把行号 → 中文短句 |

---

## 4. 数据源现状

### 4.1 主力源覆盖（基于 `marketdata_authoritative_sources.AUTHORITATIVE`）

| 类别 | 覆盖项 | 数据源 | 当前状态 |
|---|---|---|---|
| 主力资金 | 明盘净额（万）`ths_Zjl_HB` / 主力净额（手）`ths_Zjl` | TQ `get_more_info` | ✅ 已对齐 |
| 主力资金 | 大单净额（≥30 万）| thsdk `big_order_flow` | ✅ 已对接 |
| 主力资金 | 暗盘（拆单/对倒）| thsdk `tick_super_level1` | ⚠️ 已实测，待盘中切主笔级 |
| 主力资金 | 暗盘（盘后权威）| TDX `.tck` a28/a32 | ✅ 已校准（+898 万 vs 真实 +939 万，残差 <1%）|
| 主力资金 | 暗盘兜底 | 腾讯逐笔 + K 线分摊 | ✅ 兜底降级链已固化 |
| 盘口 | 十档盘口（无委托号）| TDX `.img` | ✅ 已对接 |
| 盘口 | 5 档盘口 | thsdk `depth` | ✅ 已对接 |
| 盘口 | 委托队列（字段 64）| TDX `.img` | ⚠️ 已解析，`get_order_book_queue` 仍为 TODO |
| K 线 | 日/周/月/分时 | thsdk `klines` | ✅ 已对接 |
| 全市场 K 线 | 全量 15 年（5000 行限制）| thsdk | ✅ 已对接 |
| L2 逐笔 | `tick_super_level1` | thsdk | ⚠️ 待盘中验证 |
| L2 逐笔 | 主动侧 .tck | TDX | ✅ 已对接 |
| 全市场公式扫描 | `formula_process_mul_zb` | thsdk | ⚠️ 路径已通，公式定义待做 |

**结论**：数据源**充足**，主要缺口是 thsdk 全市场公式批量（用于阶段1 阶段2 三指标共振全市场扫描）和盘中主笔级切换。

### 4.2 单位硬约束（Hermes 红线）

- **金额 = 元**（所有模块统一遵守）
- **成交量 = 股**
- 显示层自动单位切换：万 / 亿（基于 v0.4.62 修复的 `main_intent` 单位选择器）

---

## 5. 重构热点清单（按 P0/P1/P2 分级，共 15 条）

### P0（必须立即做 — 5 条）

| # | 文件位置 | 影响面 | 改造成本 | 与决策先锋规格对应章节 | 备注 |
|---|---|---|---|---|---|
| 1 | `src/core/chat_tools.py` L66-L91 `WENCAI_TEMPLATES` | AI 助手工具集：get_market_scan / get_stock_screen 选股 | 小（新增 3-5 行模板）| §三指标共振 §"三指标共振选股" | 新增"买共振/卖共振/选股共振"3 模板 |
| 2 | `src/core/dark_pool_flow.py` L125-178 `compute_pool_flow` + `src/core/market_scan.py` L155-167 `dark_top` | 全市场三榜扫描 + 单股选股 stock_pool | 中（新增 `compute_pool_flow_series` + `series_to_signal`）| §主力资金 §"1/3/5 日周期" §"上穿/下穿 0 轴定多空" | 1/3/5 日聚合=sum(daily)；CROSS=0 信号事件流；market_scan.dark_top 替换为真实暗盘 |
| 3 | `src/core/chat_tools.py` L487-497 `get_dark_flow_precise` | AI 助手 .tck 主笔级暗盘还原 | 中（新增 tck→暗盘算法）| §主力资金 §暗盘资金 §"暗盘（精确）" | 关键：parse_tck 已就绪，串联 a28/a32 委托号级 1:1 还原 + 主笔被动侧 maker 未落盘标记 |
| 4 | `src/core/ai_client.py` + 全局引用（`src/agents/base.py:299`, `src/agents/daily_report.py:697`, `src/agents/news_digest.py:479`, `src/agents/premarket_outlook.py:986`, `src/agents/intraday_monitor.py:1826`, `src/web/api/chat.py:_run_tool_loop@1935-1990`）| 反证层之外所有 LLM 调用点 | 中（提取全局熔断器 + 指数退避到 AIClient 内）| —（AI 治理）| audit_trading_agents_ai §3 标"全局缺口"；429 风暴防护 |
| 5 | `src/core/dark_flow.py` L575-L630 口诀⑥⑦（腾讯口径下退化为死代码）| 主力意图卡片 + 反证层输入 | 中（替换门控条件为量比/振幅）| §主力意图 七口诀（辅助）| 审计 P0：⑥ 永不触发，⑦ 仅靠价格不动；按 `audit_main_intent_20260825.md` §6.1 建议 1+2 |

### P1（重要 — 5 条）

| # | 文件位置 | 影响面 | 改造成本 | 与决策先锋规格对应章节 | 备注 |
|---|---|---|---|---|---|
| 6 | `src/core/resonance.py` 末尾 + `src/web/api/stock_pool.py` _screen | 前端展示 + AI 文案 | 小 | §GS §"实战文案 GO/STOP" §三指标共振 §"实战文案" | 增加 `state_action_label()`：行号→中文短句 "GO↑/持续关注/警惕主力出货/注意风险" |
| 7 | `src/core/ohlc_dark.py` 周期聚合 + `src/core/dark_pool_flow.py` 1/3/5 日聚合 | 共振选股 + 资金 0 轴 CROSS | 小 | §主力资金 §"1/3/5 日周期" | dark_pool_flow 增加 series + CROSS detector，复用 ohlc_dark 的 days=3/5 已有路径 |
| 8 | `src/core/dark_flow.py` L52-57 + `src/agents/intraday_monitor.py` L109-115 双处独立实现 strong_absorb 判据 | 全链路判据一致性 | 小（提取常量到 dark_flow，intraday_monitor import 引用）| —（治理）| audit_main_intent §6.2 建议 5；500e4/35/48 三常量两端同步 |
| 9 | `src/core/dark_l2.py` L359-383 `fetch_l2_ticks` | 暗盘主线切 thsdk | 中 | §主力资金 §"暗盘（精确）" | 环境开关 `PANWATCH_DARK_SOURCE=thsdk` 开启盘中主笔级；当前默认 tencent_ticks |
| 10 | `src/agents/intraday_monitor.py` L466-577 `_ai_counter_check` + L51-140 `_main_intent_both_inner` 中 v14 判据 | 反证层 + 主力意图读数 | 小（提取 `_derive_direction` 为跨文件共用函数，已在 L446-463 局部实现，需 export）| —（治理）| audit 建议：消重 + drift 防护 |

### P2（可选 / 中长期 — 5 条）

| # | 文件位置 | 影响面 | 改造成本 | 与决策先锋规格对应章节 | 备注 |
|---|---|---|---|---|---|
| 11 | `src/web/api/decision_pioneer.py`（1.6KB，仅 stub）+ 主 API 接入 | 前端 KPI 板块 | 小 | §前端 三指标卡 | 当前文件极小，需串联 gs/activity/fund_flow 三组 → 完整快照 |
| 12 | `src/core/dark_pool_flow.py` L172-178 `coverage` 字段 + 上层展示 | 前端覆盖率提示 | 小 | §主力资金 §"明/暗都得" | 已聚合 "full/ming_only/dark_only/none"；前端展示即可 |
| 13 | `src/agents/intraday_monitor.py` 主力意图渲染层（前端 Stocks.tsx 渲染暗盘卡片）| 前端个股对话框 | 中（前端工作为主）| §主力意图 §"卡片展示" | audit 建议 8：新增 DarkFlowCard 组件；后端数据已齐全（main_intent + main_intent_structured + mnemonic）|
| 14 | `src/agents/chat/tools_thsdk.py`（21 行超薄壳）| 治理 | 小 | — | 实际逻辑在 `src/core/chat_tools.py`，规格应改写 |
| 15 | `src/core/market_scan.py` `with_zljc` 路径接入 thsdk 全市场公式 | 全市场扫描升级 | 大 | §三指标 §"全市场扫描" | `formula_process_mul_zb("三指标共振")` 真实批量取代"单股循环 ~慢~" |

**汇总**：P0=5 / P1=5 / P2=5 = **15 条改造**。其中：
- 直接对接决策先锋规格的：#1 / #2 / #3 / #6 / #7 / #9 / #11 / #15
- 治理 / AI / 全链路补强：#4 / #5 / #8 / #10 / #12 / #13 / #14

---

## 6. 关键发现与建议

### 6.1 可直接复用的模块（无需改或仅小改）

- ✅ `decision_pioneer.py` — 三指标算法内核（已被 gs_strategy/ai_activity/market_scan/stock_pool 完整复用，公式与官方已对齐）
- ✅ `gs_strategy.py` / `ai_activity.py` / `resonance.py` — 阶段1 五件套 ①④⑤，**test_five_pack 完整覆盖 4 态 + 7 行 + 缺输入降级**
- ✅ `dark_pool_flow.py` — 主力资金聚合（明盘+暗盘），数学自洽已通过官方三例验证（中航产融/欣锐科技/杭州热电）
- ✅ `dark_l2.py` — L2 入口层（三源抽象）
- ✅ `dark_split.py` — 纯函数层（方向解码 + 明/暗拆分），无 IO 易测
- ✅ `ohlc_dark.py` — OHLC 对照项，硬标记 `approximation=True` 防误用
- ✅ `chip_distribution.py` — 三层降级（腾讯/新浪+三角分布）
- ✅ `marketdata_authoritative_sources.py` — P0 数据源定位表（Hermes 派活实现）
- ✅ `ai_client.py` — 全局 LLM 熔断器（process-level service-bucket），反证层已用上

### 6.2 需要扩展 / 重写的模块

- 🔧 `chat_tools.get_dark_flow_precise`（条目 #3）— TODO 骨架，需补真实 .tck 主笔级还原
- 🔧 `chat_tools.get_order_book_queue`（条目备）— TODO 骨架，需补托压单形态识别（字段 64）
- 🔧 `market_scan.dark_top`（条目 #2）— 当前用 OHLC 对照 hard-mark，需替换为真实暗盘
- 🔧 `chat_tools.WENCAI_TEMPLATES`（条目 #1）— 缺三指标共振选股 3 模板
- 🔧 `intraday_monitor / daily_report / premarket_outlook / news_digest / chart_analyst` 的 LLM 调用层（条目 #4）— 全部走 AIClient 裸调，需经全局熔断

### 6.3 缺失的核心能力

1. **1/3/5 日暗盘 SERIES** — `dark_pool_flow` 只算当日，无 series（决策先锋规格反复强调"三日净/五日净"和"绿转红上穿0轴"）
2. **资金 0 轴 CROSS 事件流** — `resonance.fund_flow_label` 给当前 direction，无历史 CROSS 事件
3. **三指标共振 wencai 模板** — 21 个 wencai 模板齐全，但缺"买共振/卖共振/选股共振" 3 条核心
4. **GO/STOP + 实战文案 API 字段化** — 算法层 4 态已分，前端文案需 API 字段化
5. **盘中 thsdk 主笔级暗盘** — 数据已实测验证，但盘中默认仍走腾讯逐笔

### 6.4 数据源是否充足？

**充足。** thsdk L2 + TQ more_info + 腾讯 + wencai_nlp + 本地 .tck/.img 五源已固化在 `marketdata_authoritative_sources`；降级链明确；thsdk 主笔级切换 + 全市场公式批量是主要补口。

唯一缺口：thsdk 全市场 1/3/5 日暗盘 API（可能要走 `formula_process_mul_zb(formula=暗盘三日净 + 暗盘五日净)` 形式）。

### 6.5 LLM 调用是否需要调整？

**需要。** 审计报告（`audit_trading_agents_ai_20260825.md` §3）已明确"全局 LLM 限流/熔断器缺失"是 P0 风险；AIClient 内已实现 `GlobalLLMCircuitBreaker`（进程级 service-bucket 令牌桶 10/min + 600s 冷却 + 指数退避），但**只在反证层使用**；其他 5+ 个调用点裸调 `client.chat()`。

**建议**：将熔断器做成 `AIClient.__init__` 内置（条目 #4），强制所有调用走熔断；429 由单点拦截升级为全平台协调。

---

## 7. 风险与依赖

| 风险/依赖 | 影响 | 应对 |
|---|---|---|
| **盘中实测未完成**（9-02 09:30 计划）| thsdk `tick_super_level1` 主笔级切换未确认 | 等盘中验证；如失败则继续走腾讯逐笔 + .tck 盘后校准 |
| **.tnf 实时缓存逆向失败**| 被动 maker 侧委托号（23.2% 缺口）无法获取 | 暂缓（工作量巨大）；委托级还原只覆盖主动侧 76.8% |
| **xiaoze 红 5 黄 3 修复 diff 未到**（2026-09-02 11:53Z 报告 0 附件）| 主力意图相关修复未合并 | 已回信等 diff |
| **v0.4.63 CI 未验证**| 当前生产 v0.4.62 容器崩溃，公网 500 | 阻塞；需立即部署 v0.4.63 |
| **小主机单点故障**| TdxW / 网络 / dockerd 任一挂则公网 502 | 长期 P1 改进（健康监测 + 自动 fallback）|
| **thsdk 第三方库依赖**| 维护风险 | 抽象数据层（多源 fallback 已就绪）|
| **暗盘 AI 黑盒不可知**| 暗盘精度受限 | L1 近似 + L2 精确双轨，已对齐金健米业等案例 |
| **GS 策略算法不公开**| 信号判定条件靠逆向 | 仿编 + 自建 + 对照官方校准 |

---

## 8. 重构入口建议（Phase 2 候选项）

> 仅建议，不实施；待用户审阅本报告后决策。

### 入口候选（按推荐度排序）

1. **【推荐】先做 P0-#1 + P0-#2 + P1-#6 三件套**（≈5-7 人天）
   - **目的**：补齐"wencai 三指标共振选股" + "暗盘 1/3/5 日 SERIES" + "实战文案 API 字段化"，产出可演示的三指标共振选股 v0.4.65 demo
   - **可交付**：AI 助手能直接调"今日三指标共振选股"返回结果，前端展示共振状态 + 实战文案 + K 线 + 三副图
   - **风险**：低（算法已有，主要是数据串联 + 模板新增）

2. **次推 P0-#3 + P1-#9**：盘中 thsdk 主笔级 .tck 暗盘还原上线（≈7 人天）
   - **目的**：打通盘中主笔级暗盘精度，把 `confidence=L1_approx` 升为 `official`
   - **依赖**：9-02 09:30 盘中实测通过 + 环境开关 `PANWATCH_DARK_SOURCE=thsdk` 配置完成
   - **风险**：中（环境切换影响生产）

3. **备选 P0-#4**：AIClient 全局熔断强制接入全链路（≈3 人天）
   - **目的**：429 风暴防护，治理全平台 LLM 调用
   - **依赖**：refactor 全量 5+ 调用点，可能引发回归
   - **风险**：中（回归测试覆盖要全）

4. **长期 P2-#15**：接入 thsdk 全市场公式扫描（≈10 人天）
   - **目的**：把全市场扫描从"单股循环 ~慢~"升级为 thsdk 公式批量
   - **依赖**：thsdk `formula_process_mul_zb(formula=三指标共振)` 公式定义 + .tn6 加密绕过 / 自定义公式支持
   - **风险**：高（thsdk 公式限制可能不支持三指标复杂判定）

### 推荐下一步

**Phase 2 启动建议**：入口 #1（P0 #1+#2 + P1 #6 三件套），预计 v0.4.65 demo 版；交付后老板决策是否进入入口 #2（盘中主笔级）。

---

## 9. 附录

### 9.1 复刻算法与官方基准对照（必须达标）

| 算法 | 官方基准 | SIDA-Pro 现状 | 验证方法 |
|---|---|---|---|
| 活跃度（7 因子 MAX×1.2）| 截图对齐 | `compute_institution_activity` | 与官方截图数值误差 ≤ 0.1 |
| GS 信号（G/S）| 神剑股份截图 | `compute_gs_signal` | 与官方截图 G/S 出现时机近似 |
| 共振向好上涨概率 | 75.42% | `evaluate_state` row=1 | 历史回测 ≤ 2% |
| 共振拐点上涨概率 | 73.48% | `evaluate_state` row=2 | 历史回测 ≤ 2% |
| 共振分歧上涨概率 | 70.55% | `evaluate_state` row=3 | 历史回测 ≤ 2% |
| 共振走坏上涨概率 | 67.40% | `evaluate_state` row=7 | 历史回测 ≤ 2% |
| 向好盈亏比 | 3.45 | 待回测 | — |
| 大单阈值 | 30 万 | `ming_net_from_big_orders` | 与 thsdk 扩展 1 主力净流入误差 ≤ 5% |
| 暗盘主导（杭州热电）| 明盘 -0.43 / 暗盘 +1.33 | `_dark_flow` v4 | 案例对齐 |
| 暗盘主导（中航产融）| 明盘 +1887.3 / 暗盘 +1052.8 | `_dark_flow` v4 | 案例对齐 |
| 暗盘背离（欣锐科技）| 主力 -9674.1 | `_dark_flow` v4 | 案例对齐 |

### 9.2 测试约定

`tests/test_five_pack.py`（173 行）是阶段1 五件套 ①④⑤ 的标准单测范式：
- 用 `pytest`
- 用 `_mk_bars(n=40, trend=..., final_surge=..., final_crash=...)` 生成合成 K线
- 覆盖正/反/边界 + 缺输入降级 + 跨日合并（同日 vs 前值）
- 全部断言精确字段值

重构时新增模块应继续按此风格编写（`test_chat_tools_p1p2.py` 已存在 / `test_market_scan.py` 已存在 / `test_dark_pool_flow.py` 已存在）。

### 9.3 引用文档速查

| 代号 | 路径 |
|---|---|
| GD | `C:\Users\tianxiang\决策先锋8问8答_解析.md` |
| FX | `C:\Users\tianxiang\同花顺决策先锋复刻研究手册.md` |
| TDX | `C:\Users\tianxiang\通达信客户端研究结果汇总-20260830.md` |
| ZS | `C:\Users\tianxiang\Desktop\同花顺决策先锋核心功能详细解析，包括用到了哪些指标.md` |
| GSXH.txt | `Desktop\三指标公式\4_GSXH_GS信号.txt` |
| APZJ.txt | `Desktop\三指标公式\3_APZJ_暗盘资金.txt` |
| JGHYD.txt | `Desktop\三指标公式\1_JGHYD_机构活跃度.txt` |
| NXX.txt / NXXZT.txt | `Desktop\三指标公式\2_NXX_牛熊线.txt` / `8_NXXZT_牛熊线主图.txt` |
| GZZH.txt | `Desktop\三指标公式\5_GZZH_三指标共振.txt` |
| QSCPX.txt | `Desktop\三指标公式\6_QSCPX_趋势操盘线.txt` |
| GSXHZT.txt | `Desktop\三指标公式\7_GSXHZT_GS买卖点主图.txt` |

---

*整理人：ZCode Marvis · 整理日期：2026-09-02 · 仅供研究参考，不构成投资建议*
*未做任何代码修改；扫描覆盖核心 8 个 + 优先 3 个 + 抽样 4 个 + 测试 2 个 + 复刻公式 4 个 + 审计报告 2 个 = 23 份文档/源码*
