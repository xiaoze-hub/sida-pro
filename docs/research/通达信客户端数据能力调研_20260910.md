# 通达信客户端(本机 TQ 网关)板块数据能力调研

> 日期: 2026-09-10 晚 | 场景: 板块热力图/异动实时数据源评估(老板提议"通达信客户端更快、不限流")
> 结论先行: **可用, 且批量能力远超 thsdk**；但两者"板块"目录口径不同(代码撞号不同义), 接入须先做映射。

## 1. 接入现状(已存在, 非新链路)

- 本机 Windows 跑 **通达信金融终端** `TdxW.exe`(装于 `c:\new_tdx64`), 自带 HTTP 服务监听 **127.0.0.1:17709**, 宿主网卡侧 `172.27.16.1:17709`, 生产容器(panwatch)已可达。
- 仓库已有 vendor: `packages/marketdata/src/marketdata/vendors/tq.py`(`_rpc`/`tq_rpc`, 地址自动发现; 生产注入 `TDX_QUANT_URL=172.27.16.1:17709`), 现用于个股快照/K线/公式(ZLJC 主力进出)。
- 客户端插件源码 `c:\new_tdx64\PYPlugins\user\tqcenter.py`(4118 行)即为服务端方法全集; 网关按白名单放行(未知方法报 `-32601`), **板块类方法多数已放行**。

## 2. 实测能力(2026-09-10 晚, 收盘后)

| 能力 | 方法 | 实测耗时 | 说明 |
|---|---|---|---|
| 板块目录 | `get_sector_list(list_type=1)` | 236-363ms | 返回 **587 个板块** {Code,Name}: 880xxx 概念/地区/风格 459 个 + 881xxx 二级行业 128 个 |
| 全板块实时价 | `get_pricevol(stock_list=[587])` | **84ms(容器内) / 324ms(本机)** | 每码 `{LastClose, Now, Volume}` → 涨幅=Now/LastClose-1 |
| 成交金额 | 公式 `AMO` 批量 | ~300ms/587 | `AMOW`=当日成交额(万元); 与单码快照 `Amount` 一致(881290: 156.6亿) |
| 成交量+5日均量 | 公式 `VOL` 批量 | ~230ms/587 | `VOLUME` / `MAVOL2` → **量比可自算** |
| 量比 | 公式 `SG-LB` 批量 | ~270ms | 直接给`量比`+MA5/MA10; 概念板块值偏大(实测 40+), 需校准 |
| 换手 | 公式 `HSL` 批量 | ~250ms | `HSL` 换手率% |
| 主力资金 | 公式 `ZJLX` 批量 | ~2.2s/587 | `主买净额`(万元, 881290 +7.9亿 / 代糖概念 -17.2亿) + 近5/10日流向 |
| 主力资金2 | 公式 `SUPAMO` 批量 | ~220ms | `主力资金`(万元) + 超大单占比% |
| 主力进出三档 | 公式 `ZLJC` 批量 | ~1.5-2.5s/587 | JCL/JCM/JCS 净量(仓库已标注"单位未校验", 仅作代理口径) |
| 板块成分股 | `get_stock_list_in_sector('881290.SH')` | ~270ms | 11 只, 标准 6 位代码带市场后缀 |
| 个股所属板块 | `get_relation('600519.SH')` | ~345ms | 返回 BlockCode/BlockName/BlockType(行业/地区/概念/风格) |
| 单码快照(含**涨速**) | `get_market_snapshot('881290.SH')` | ~230ms/码 | 含 `Zangsu`涨速 / 涨跌家数 UpHome/DownHome / Amount / 买卖盘;**不支持批量** |
| 板块日K | `get_market_data(['881290.SH'],'1d')` | ~260ms | 日线可用于回填/校验 |
| 板块分钟K | 同上 period=1m | ❌ | 本地**缓存陈旧**(实测仍在 2026-05-27), `refresh_kline` 无效 → 分时量比不可得 |

对照 thsdk(当前生产实时链路): 3 码快照 2.6s; 480 码全量约 10s(60s 缓存+单飞)。**TDX 全量涨幅+成交额+资金+量比 ≈ 3s 内**, 且为本地客户端无配额/限流问题。

## 3. ⚠️ 关键坑: 两套"板块"口径不同(撞号不同义)

- 同花顺(thsdk, 现生产库 `Boards` 480 条, `URFI` 前缀): **881155 = 银行**(今日 +1.47%)。
- 通达信: `get_match_stkinfo('881155')` → **881155.SH = 其他纺织**; TDX 银行类只有 三个板块(全国性银行/地方性银行/中小银行)。
- 名称对账覆盖率: 行业 **51/90**、概念 **157/390**(严格同名); 直接按代码对仅 18/90(且不可信 —— 撞号不同义)。
- 结论: **绝不能按代码映射**, 必须按"名称 + 成分股重合度"建一次性映射表(成分股两侧都能取: TDX `get_stock_list_in_sector` / thsdk `fetch_block_constituents`), 未命中的板块保留 thsdk 兜底。

## 4. 集成方案(待老板拍板)

- **方案A(推荐)**: 映射表(TDX↔同花顺, 名称严格 + 成分股重合度自动生成, 人工复核一次) → 热力图实时链路改 **TDX 优先**(pricevol + AMO/ZJLX/SG-LB 批量, ~3s/全量), 未映射/取数失败回落 thsdk; 涨速用"我们自己的 60s 轮询价差"自算(5 分钟窗口), 或对 Top N 异动候选调单码快照取官方 `Zangsu`。板块口径与大屏口径保持现状。
- **方案B**: 热力图直接用 TDX 587 板块原生目录(口径最干净、最快、全覆盖), 但板块详情/成分股/日线历史(board_daily)与页面持仓/同花顺口径分叉, 改造面大。
- **方案C**: 保持 thsdk 主链路, 仅把 TDX 作为**批量兜底/加速**(thsdk 抖动或限流时顶上), 改动最小。

## 5. 风险与未决

1. **陈旧快照防护**: 客户端未更新时 TQ 会返回旧数据却报成功(2026-09-03 生产事故教训) → 板块链路同样要带新鲜度校验(如 `get_more_info.HqDate` / 日K最新日期)。
2. **依赖客户端常开**: 生产 TQ 链路已依赖 TdxW.exe; 板块链路加入后该依赖加重(failover 设计需保留)。
3. **单位/口径校准**: ZJLX 主买净额 vs thsdk 主力净流入定义不同(代糖概念 -17.2亿 vs -8.4亿), 展示层需明确口径或换算, 不混用。
4. **涨速批量缺位**: 单码快照 230ms×480≈2 分钟不可行 → 自算(方案A)为准。
5. 公式历史值(return_count>1)返空(仓库既有结论), 只能取最新一根。

## 6. 复现

- 探测脚本(一次性): `%TEMP%\probe_tq_blocks.py` / `probe_tq_blocks2.py`(本机)。
- 最小验证: `POST http://127.0.0.1:17709 {"id":1,"method":"get_pricevol","params":{"stock_list":["881290.SH"]}}`。

## 7. 客户端页面走查(2026-09-10 晚, 老板要求)

版面(顶栏页签): **板块指数 / 板块云图 / 港澳美股云图 / 板块异动 / 板块轮动 / 相对轮动图 /
经济区板块 / 国资系板块 / 民营系板块 / 投资参股板块**(均在"版面"菜单下, 主菜单: 行情/市场/
资讯/数据/发现 + 功能/版面/公式/选项)。

各页实测要点:

1. **全景图**: 指数迷你图 + 沪深A股涨跌幅/涨速榜 + 右侧**板块行情**(全部/行业/概念/风格 四档,
   表: 代码/名称/涨幅%) + 24小时滚动资讯 + 分时/K线。板块行情榜与 API `get_pricevol` 数值一致。
2. **板块云图**: **两级 treemap**(面积=总市值/颜色=当前涨幅, 市场可选), 一级=行业板块(带总市值),
   二级=板块内代表个股; 提示"双击方格可进入行情"。→ 与 SIDA 热力图同构, 验证我们的设计方向。
3. **板块异动**(老板截图的页): 上表 = 板块列表, 列: 板块代码/名称/涨幅%/现价/最新异动/**异动类型**
   (涨异动/跌异动)/异动日涨%/异动天数(最新异动至今)/异动天数(平均周期)/**距下次异动**(=平均周期-至今,
   计算列)/**轮动系数**/近一年异动次数/涨异动数/跌异动数; 下表 = 选中板块的**关联个股**, 列:
   代码/名称/**涨幅%**/**涨速%**/换手%/总金额/A股流通市值/PE(TTM)/PB(MRQ)/近一日涨%/近一周涨%/近一月涨%。
   数据源 `cfg_bk_bkld_yjhy`(**云数据**, 需通达信数据权限, TQ 云数据接口返回空 → 暂不可用)。
4. **板块轮动**: 左=市场板块列表(全部/行业 + 板块地图); 中=日期页签(09月10日/09日/08日/07日…)
   的板块排名, 列可切 **涨跌幅/涨停数/主力净额/5日涨幅**; 右="关联个股"(选中板块, 含涨幅排序);
   另有"每日热点预测/昨日热点今日涨幅/连板情况(四板+/三连板/二连板)/最赚钱板块及个股(近一周)"
   (云数据 web 页, 来源 page.tdx.com.cn:7615 内嵌)。
5. **港美股云图**: 港/美个股与板块 treemap + 恒生/国企指数统计(涨跌家数)。
6. 经济区/国资系/民营系/投资参股: 同"板块轮动"布局的**不同板块域**变体(按区域/股权属性过滤)。

**可 API 复刻的**(本次已落地, 见 §8): 板块目录、板块/个股批量实时(价/额/资金)、成分股、名称表、
涨跌停(ZDT)/日历等。**只能看不能取**: 异动类型/异动天数/轮动系数等云数据(需权限),
以及"热点预测/连板情况"等云页面内容(web 内嵌, 未走 TQ)。

内嵌页地址规律(留档): `http://page[1-3].tdx.com.cn:7615/site/pcwebcall/html/pc_tcld_*.html`
以及 `page1.tdx.com.cn:7615/site/tdx-pc-ggzx/page-zx.html?...&typeid=bkxw`(板块新闻/研报)。

## 8. 方案B 落地(2026-09-10 晚, 老板拍板"就用通达信, 盘中耽误不得")

- **后端**: 新增 `src/core/tdx_boards.py`(目录/批量实时/成分股/名称表/新鲜度/涨速自算/量比代理)
  + `/boards/heatmap?source=auto|tdx|ths`(auto=通达信优先, 不可用或数据陈旧回落 thsdk)
  + `/boards/{88xxxx.SH}` 详情 + `/boards/{88xxxx.SH}/constituents`(每只**涨幅**/现价/成交额)。
- **实测**(真实客户端, 收盘后): 行业 128 个 2.2s、概念 457 个 3.2s(60s TTL+单飞, 二次 11ms);
  成分股 11 只 1.0s, 涨幅与客户端"关联个股"面板逐字一致(国瑞科技 +6.92% 等);
  资金=SUPAMO 主力资金(与 thsdk 主力净流入口径接近: 代糖概念 -8.85亿 vs -8.42亿), 展示层标注"通达信主力资金"。
- **量比**: 无批量源 → 代理 = 今日成交额/(前5日均额×时段进度), 基准当日缓存; **涨速**: 自身 60s 轮询价差自算(5 分钟窗)。
- **待办**: 云数据(异动类型/轮动系数)需确认客户端数据权限; 板块轮动(rotation 接口)仍为 thsdk 口径,
  通达信板块 5 日涨幅暂显示 --。

## 9. 龙虎榜(2026-09-10 深夜, 老板要求"龙虎榜也可以接入吧")

**结论: TQ 云数据接口取不到, 但客户端页面缓存可以读。**

- 云数据侧(不可用): 用当日真上榜 8 只(605398/605177/603958/603601/603421/603216/688496/920268)实测 —— `上榜资金` 公式全 0(口径/权限), `get_gpjy_value`/`get_scjy_value` 以榜单字段(bzb/szb/jmr/zmr/zmc/sl1/sl2/lx)查询全 null; 与板块异动同因(需通达信数据权限, KI-048)。
- **本地缓存侧(已落地)**: 客户端打开「龙虎榜」页面后数据落盘 ——
  - `T0002/cloud_cache/list/func_lhbfx101_1.jsn`(GBK JSON, 榜单): 列 `$ZQDM1/$SC1/date/bzb(买占比)/szb(卖占比)/jmr(净买入)/zmr(买入合计)/zmc(卖出合计)/sl1(陆股通席位)/sl2(机构席位)/lx(异动类型)/lb(3日上榜)/$ZQDM(关联ID)`。
  - `T0002/cloud_cache/lhbfx/<关联ID>.jsn`(席位明细, 点开个股才落地): `yyb(营业部, 含"买(1): 深股通专用")/bje/sje/jmr/zb(占比)/mrcgl1·3·5(买入后1/3/5日成功率)/ygcb(预估成本)/ygsy(预估收益)`。
- 同步链(容器读不到 Windows 文件): WSL 可读 `/mnt/c/...` → `scripts/sync_tdx_lhb_cache.sh`(WSL root, 幂等, 仅变更拷贝) → panwatch 数据卷 `/app/data/tdx_lhb` → 应用解析。注册 5 分钟计划任务待老板确认。
- API: `GET /api/archive/dragon-tiger-tdx`(榜单) / `GET /api/archive/dragon-tiger-tdx/seats?ref_id=`(席位明细; 无缓存 `available=false`)。
- 实测(2026-09-10 23:04 老板点开页面): 榜单 64 条 / 席位明细 12 条(002636 深股通专用净买 1.40亿), 与东财库内名单交叉一致。
- **对照**: 我们已有东财 `dragon_tiger_events`(241 日/2 万行, 已喂妖股因子)覆盖榜单字段; 通达信增量 = **机构/陆股通席位数、成交占比、席位级买卖明细与胜率**; 东财侧另有机构买卖统计/营业部明细报告可作替代(已验证可达)。

- **后续(2026-09-10 深夜, 老板"切换到东财全自动方案")**: 通达信缓存路径(v0.5.54)保留为补充; 主线改为东财全自动 —— 迁移 v160 两表
  (`lhb_institution_daily` 机构买卖统计含上榜后 1/2/3/5/10 日涨幅; `lhb_seat_details` 营业部席位明细含 3 日上涨概率),
  cron 交易日 17:50 增量, API `/api/archive/lhb-institution` 与 `/api/archive/lhb-seats`。
