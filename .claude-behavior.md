# Behavioral Guidelines (adapted from Karpathy + SIDA conventions)

> **来源**: 基于 `forrestchang/andrej-karpathy-skills` 的 `CLAUDE.md` (204k ⭐ MIT)，结合 SIDA 生产约束改写。
> **取舍**: 这套规则偏稳健、不偏快。改动前先思考，遇到模糊就停下问。
> **Working if**: 改动的 diff 更聚焦，返工减少，明确提问先于错误发生。

## 1. Think Before Coding — 不要假设，暴露权衡

实现前:
- **显式说明假设**。不确定就问，**不要为了"答得快"瞎编**(尤其数据基准日、接口字段、阈值)。
- 多解并存时**列出选项**，不要默默选一个。
- **数据缺口 = 显式标注** ("数据缺失"标签)，**禁止 LLM 推测**(幻觉敏感)。
- **基准日滞后 → UI 显式标注** ("数据截至 XX:XX")，不偷偷用旧数据推断今天。

## 2. Simplicity First — 最小代码解决问题，不投机

- 不实现**未被要求**的功能。
- 不为单次使用抽公共方法。
- 不加**未被要求**的可配置性/灵活性。
- 不为不可能发生的场景写错误处理。
- 写 200 行能 50 行解决 → 推倒重写。
- 写之前自问：**"ROI > 复杂度吗?"** SIDA 偏好:能用一个文件解决就别拆三个。

## 3. Surgical Changes — 只动必要的，清掉自己的残渣

修改既有代码:
- **不"顺手优化"相邻代码、注释、格式**。
- **不重构未坏的部分**。
- 匹配现有风格，即使你个人觉得别的更好。
- 发现**无关的**死代码 → **指出来**，不要删。

热修 cron / 生产 bug 时:
- diff 必须聚焦到根因，禁止"顺手改 5 个文件"。
- docker cp 热修 ≠ DB 修复 → 镜像重启前必须复检 DB。
- 失败立即回滚，**不要继续堆补丁**(参见 CHANGELOG "热修"条目)。

测试:每个改动行必须能追溯到用户请求。

## 4. Goal-Driven Execution — 定义成功标准，循环到验证

任务转可验证目标:
- "加个验证" → "写测试覆盖非法输入 → 让它们通过"
- "修 bug" → "写复现测试 → 让它通过"
- "重构 X" → "重构前后测试都过"

多步任务先列计划:
```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

SIDA 专属门禁:
- **数据改动**: 必须跑通真实数据，不准用 mock 假装通过。
- **UI 改动**: 必须前端构建 + 视觉自检(防 AI 模板感)。
- **发布**: 镜像构建后必须清旧镜像(image prune)，参考 `build.sh`。

## SIDA 项目特定红线

🚫 **绝不**:
- 用 mock 数据替换真实数据接口(尤其生产环境)。
- 把未验证的 LLM 推断写进用户可见的"事实"标签。
- 在生产容器内手动 `docker exec` 改源码(必须走 `build.sh` 发版或 `docker cp` 热修流程)。
- 跳过 pre-push hook 的测试直接 `git push --no-verify`(除非紧急热修且已在 CHANGELOG 标注)。

✅ **应当**:
- 复杂任务先写 plan 到 `.hermes/plans/`，执行完更新 todo。
- 任何数据/预测结论附**数据基准日 + 数据源**。
- 引用论文/官方文档时附 URL，不编造引用。